# STATUS — рабочий лог проекта ЛАД

Обновлять в конце каждой сессии. Контекст проекта — в `CLAUDE.md`.

---

## Где мы сейчас (08.09.2026)

Система работает целиком: ввод данных → составление → проверка → выдача
(HTML и Excel). Развёрнута на **https://nekiae-lad-timetable.streamlit.app**,
пересобирается автоматически при пуше в `main` репозитория `nekiae/lad-timetable`.

**Ближайшая дата — Startup Day БГУ, ~15.09.2026**, там будут люди из
Министерства образования. Это первый публичный показ; §8.6 CLAUDE.md
(«не питчить, спросить имя») применяется именно к нему.

### Что изменилось относительно исходного плана

**Колонки «было» не будет.** Решено 06.09.2026: система принимает вводные
(классы, кабинеты, предметы, учителя, нагрузка, пожелания) и составляет
расписание. Существующее расписание школы в неё не загружается. Это отменяет
демо-сценарий §6 CLAUDE.md с тремя цифрами «было/стало» и §8.3 про сравнение
одним валидатором. Механика `render(before=...)` в коде осталась, входа для неё
не строим. Ценность показывается цифрами самого результата.

**Ввод стал процессом, а не девятью вкладками.** Завуч идёт по шагам, лента
шагов вверху показывает, где он и что осталось. Данные сохраняются на диск при
каждом изменении, а не по кнопке: ввод школы — это часы работы, и терять их
на закрытой вкладке недопустимо (500 строк сериализуются за 1,6 мс).

**Стартовые данные — школа завуча**, Жемчужненская СШ Барановичского района:
24 класса V–XI, 52 учителя, 428 строк нагрузки, 838 часов. Приложение
открывается сразу на ней, а не на пустом файле и не на гимназии.

---

## Сделано в эту сессию (08.09.2026)

### 1. Фаза улучшения доводится до конца бюджета

CP-SAT иногда завершает поиск сам, задолго до лимита, со статусом FEASIBLE —
то есть без доказательства оптимальности. Замерено 07.09.2026 на гимназии
(28 классов), бюджет 300 с, одни и те же данные:

| Прогон | Вторая фаза | Окон у учителей |
|---|---|---|
| А | 291 с из 291 | 38 |
| Б | 166 с из 294 | 52 |
| В | 25 с из 279 | **268** |

Разброс в семь раз при одинаковом вводе. Для показа это хуже, чем медленно:
нельзя нажать кнопку и не знать, что покажет экран.

Исправлено в `src/lad/solve.py`: после каждого возврата солвера, если время
ещё осталось, поиск перезапускается от лучшего найденного (старые подсказки
снимаются через `ClearHints` — повторный `AddHint` для той же переменной делает
модель невалидной). Лучшее решение держится отдельно: следующий круг стартует
с той же точки, но пойти может хуже, и наружу отдаётся не последнее, а лучшее.
Выход из цикла — доказанный OPTIMAL, исчерпанный бюджет, просьба остановиться,
мгновенный возврат солвера (значит, следующий круг будет таким же)
или страховочный предел в 8 кругов.

Замер после правки, гимназия, бюджет 300 с — см. таблицу в разделе «Замеры».

### 2. Экран результата: три главные цифры

Валидатор даёт восемь величин, и все восемь одного размера читались как
таблица — глаз не знает, с чего начать. На показе третьим лицам времени
разбираться нет ни у кого.

Наверх вынесены три цифры, ради которых всё делалось: **за сколько составлено,
сколько нарушено санитарных норм, сколько у учителей окон.** Крупно, антиквой,
в одной карточке, с поясняющей строкой под каждой («980 уроков, 28 классов»,
«на 60 человек за неделю»). Остальные пять величин — обычными метриками ниже.
Статус солвера ушёл в подпись мелким шрифтом и переведён на человеческий:
OPTIMAL → «доказано, что лучше не составить», FEASIBLE → «все требования
соблюдены; на доказательство того, что лучше нельзя, времени не хватило».

Функция `headline()` — в `src/lad/style.py`, на узком экране колонки
схлопываются в столбец (цифра в 3rem в трети телефонного экрана переносится
по цифрам).

### 3. ⚠️ Найдено: стартовые данные не решаются

Прогон школы завуча со всеми нормами в жёстком режиме даёт **INFEASIBLE
за 25–35 секунд**. То есть завуч открывает приложение, нажимает кнопку
и получает «расписания нет».

Разбор (`diagnose`) находит причину за 43 с и называет её точно:

> Кабинетов не хватает. Всего 30 на 24 класса. Класс учится без окон, поэтому
> на первом уроке заняты все классы сразу. Самые тесные: «спортзал» — 1 шт.
> на 72 ч, «физика» — 1 шт. на 40 ч, «биология» — 1 шт. на 34 ч.

Это дефект **данных, а не кода**: в файле 22 обычных кабинета на 24 класса
и один спортзал на 72 часа физкультуры в неделю. Диагностика при этом
отработала ровно так, как задумана, — назвала причину и что делать.

**Не исправлено намеренно.** Реальный кабинетный фонд Жемчужненской школы
неизвестен, а выдумывать его — ровно то, против чего написано правило
§12.3 CLAUDE.md. Варианты решения — в «Следующих шагах».

---

## Замеры

### Гимназия (28 классов, 60 учителей, 43 кабинета, 980 часов), бюджет 300 с

Все нормы в жёстком режиме, веса по умолчанию.

| Прогон | Итог | Время | Штраф | Окон у учителей | Норм | Конфликтов |
|---|---|---|---|---|---|---|
| 1 | FEASIBLE | 300 с | 3164 | **29** | 2 | 0 |
| 2 | FEASIBLE | 300 с | 3313 | **34** | 2 | 0 |
| 3 | FEASIBLE | 300 с | 2920 | **29** | 2 | 0 |
| 4 | FEASIBLE | 300 с | 2858 | **30** | 3 | 0 |

980 уроков в каждом прогоне, 0 окон у классов. Нарушения — та самая
физкультура в 11А, о которой система предупреждает до запуска.

**Что изменилось.** Было 38 / 52 / 268 окон, стало 29 / 34 / 29 / 30. Разброс
с семикратного упал до 1,2-кратного, и худший результат теперь лучше прежнего
лучшего. Все четыре прогона выработали бюджет целиком (300 с из 300) — до
правки солвер выходил сам на 25-й секунде и отдавал, что успел. Статус
FEASIBLE, а не OPTIMAL: за пять минут доказать оптимальность на 28 классах
не выходит, но расписание законно и хорошо.

### Ранее (25–26.08.2026) — три модельные школы

Генератор — `lad.demo_city`, пересборка — `tools/make_demo_schools.py`.

| Школа | Классов | Учителей | Часов | Что проверяет |
|---|---|---|---|---|
| Дубровка (сельская) | 7 | 16 | 236 | учителя-многопредметники, тесный кабинетный фонд |
| СШ № 3 (районная) | 14 | 39 | 490 | методические дни, профили X–XI |
| Гимназия № 7 (городская) | 28 | 60 | 980 | масштаб, четыре профиля, 10 методических дней |

Итог по гимназии на бюджете 10 минут: 980 уроков, 0 конфликтов, 0 окон
у классов, 40 окон у учителей на 60 человек, 3 нарушения — все три физкультура
в 11А, о которой система предупреждает до запуска. Результат проверен
независимым аудитом: `tools/audit_schedule.py` разбирает готовый HTML
и пересчитывает всё с нуля, не используя `lad.validate`.

**Воспроизводимость (до правки фазы улучшения).** Пять прогонов одних данных:
UNKNOWN 150 с (машина занята), OPTIMAL 123 с, 10,6 с, 5,8 с, 19,6 с. Солвер
работает в 8–16 потоков, обмен между ними зависит от таймингов машины, поэтому
каждый запуск идёт по дереву своим путём. Время гуляет в двадцать раз,
исход стабилен.

---

## Что уже исправлено (история дефектов)

Каждый следующий дефект вскрывался только на школе другого устройства —
поэтому модельных школ три, а не одна.

1. **Норма применялась ко всей школе разом.** Физкультура «не два дня подряд»
   при пятидневке вмещает максимум 3 занятия, в 11А их четыре — класс нерешаем
   арифметически, и из-за одного класса из 28 приходилось ослаблять норму
   целиком. Теперь послабление точечное, и система сама называет класс, причину
   и выход. На тех же данных: нарушений 32 → 2, окон у учителей 177 → 54.
2. **Бюджет по умолчанию был 30 секунд**, а первое законное расписание
   на 28 классах находится за 119 с. Стало 5 минут, минимум — минута.
3. **«Не успел» показывалось как «решения нет».** UNKNOWN и INFEASIBLE — разные
   ответы, требующие разных действий. Теперь UNKNOWN даёт «искать дольше»,
   INFEASIBLE — разбор причин.
4. **`diagnose()` мог оговорить невиновную норму** — засчитывал в вину любой
   неуспех, включая «не успел». Теперь виновной считается только доказанная
   невозможность.
5. **Один учитель на обе подгруппы принимался молча.** Подгруппы идут в один
   час, значит вести их обязаны разные люди. Ловится до запуска, с указанием
   класса, предмета и имени.
6. **Нехватку кабинетов не находил никто** — гимназия молчала 7 минут
   и отвечала UNKNOWN. Арифметика по неделе этого не видит: 645 часов
   помещаются в 17 кабинетов, но на первом уроке заняты все 28 классов сразу.
   `diagnose()` проверяет фонд экспериментом (`solve(..., ignore_rooms=True)`)
   и называет самые тесные типы за 7 секунд.
7. **Предупреждение о тесной сетке**: класс, занимающий больше 90% слотов,
   помечается до запуска — солверу не остаётся свободы на нормы.

**Урок, который стоит помнить.** Когда гимназия не решалась, разбор указывал
на два узких места сразу, и был сделан вывод, что норма о трудных предметах
конфликтует с профильным учебным планом. Вывод оказался неверным: арифметика
его не подтверждала, а причина была в кабинетах (28 классам нужно 28 обычных
кабинетов, генератор давал 20, считая по средней доле уроков в спецкабинетах —
среднее сходится, а расписание упирается в ПИК). Объяснение звучало убедительно
и было принято без проверки. **Прежде чем обсуждать нормы — считать кабинеты.**

---

## Следующие шаги

| # | Что | Зачем | Когда |
|---|---|---|---|
| 1 | Решить, что делать с кабинетным фондом стартовых данных | иначе приложение по умолчанию отвечает «расписания нет» | **до показа**, блокер |
| 2 | Вердикт завуча (§8.2 CLAUDE.md, пять вопросов) | фильтр 0 точки выхода §10 | желательно до 15.09 |
| 3 | Анонимизация в интерфейсе | на Startup Day реальные ФИО учителей показывать нельзя (§8.4) | отложено сознательно 08.09 |
| 4 | Что говорить на Startup Day | §8.6: не питчить, спросить имя того, кому показать | к 15.09 |

**По пункту 1 — три пути**, выбор за Владом:
- спросить у завуча реальный фонд (правильно, но нужен контакт до показа);
- поставить правдоподобный минимум (24 обычных + 2 спортзала) и пометить
  файл как пример — тогда цифры демо не про реальную школу;
- оставить как есть и показывать на гимназии, а школу завуча держать
  как проверку диагностики.

---

## Открытые вопросы

| # | Вопрос | Статус | Почему важно |
|---|---|---|---|
| 1 | Пишет ли ГИАЦ модуль расписания сам? | ⏸ отложено | определяет судьбу проекта (§10); запрос не шлём до вердикта завуча |
| 2 | Санитарные нормы РБ | ✅ добыты 22.08.2026 через web.archive.org, оцифрованы в `data/sanpin_by.json` (`domain.md` §12) |
| 2а | Предельная недельная нагрузка и длительность урока | ✅ закрыт 23.08.2026: типовой план № 75, урок 45 мин (ССЭТ № 525 прил. 12) |
| 2б | Максимум уроков в день | ❗ прямой нормы нет ни в одном документе — проверены СанПиН, ССЭТ с приложениями, типовые планы, ИМП. Выводится косвенно |
| 2в | Расхождение первоисточников по нагрузке | ⚠️ прил. 16 ССЭТ vs типовой план № 75 (IX: 33/34, X–XI: 34/35). Взят план, расхождение в конфиге. **Спросить у завуча, какой цифрой пользуется школа** |
| 3 | Реальный кабинетный фонд школы завуча | ❗ **новое, 08.09.2026** | без него стартовые данные нерешаемы (см. выше) |
| 4 | Входит ли расписание в дорожную карту «Электронной школы»? | ❓ по открытым источникам не видно | сайты edu.gov.by / giac.by не открываются из-за границы |
| 5 | Вердикт завуча: можно ли ставить в работу? | ❓ | фильтр 0 точки выхода (§10) |

Вопрос «есть ли доступ к реальному расписанию школы» **снят** — импорт
расписания не делаем (см. «Что изменилось»).

## Заметки

- Сайты `.by` (edu.gov.by, giac.by, schools.by, adu.by) отдают ECONNREFUSED
  из-за границы. Открывать вручную или искать зеркала.
- Цифры по нормам НЕ выдумывать. Не подтверждено → `UNVERIFIED`.
- После правки модуля в `src/lad/` сервер Streamlit надо **перезапустить**:
  `runOnSave` перечитывает только `app.py`.
- Расписания трёх модельных школ и Excel — в `out/demo/` (не под гитом).
