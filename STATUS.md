# STATUS — рабочий лог проекта ЛАД

Обновлять в конце каждой сессии. Контекст проекта — в `CLAUDE.md`.

---

## Каналы выхода — статус на 21.09.2026

Со слов Станислава Тарасюка:

- **Минобразования** — звонок назначен на **18:00 21.09.2026**. Первый живой контакт
  по цепочке ПВТ → министерство, раньше ожидавшихся «30–40 дней».
- **Schools.by** — ответа на обращение нет. Станислав нашёл контакт одного
  из руководителей компании и пишет ему напрямую.

Оба питча по три минуты готовы:

| Куда | Дек | Текст речи |
|---|---|---|
| Schools.by | https://claude.ai/artifact/LXt2UWPP3FEtdv8osQLsWC | `docs/PITCH_SCHOOLSBY.md` |
| Минобразования | https://claude.ai/artifact/H2M348GH1QADD8FCu5UghE | `docs/PITCH_MINOBR.md` |

Выгрузки PDF и PPTX (с заметками докладчика) — `out/pitch/`, в git не хранятся.

⚠️ Вердикта завуча по-прежнему нет — фильтр 0 из §10 CLAUDE.md не пройден.
На обеих встречах называть это самим.

---

## Где мы сейчас (14.09.2026) — спринт продукта

Режим сменился: через Startup Space о проекте узнал министр — «делай продукт».
Пять дней, до **19.09.2026**. План, архитектура и порядок урезания скоупа —
`docs/PLAN.md`. Streamlit остаётся запасным вариантом до 19.09.

### Сделано 14.09

1. **Стартовая школа не составлялась вовсе** — в закоммиченном `data/school.json`
   с 7109792 (08.09) была пустая таблица учителей, все 428 строк нагрузки
   падали с «учитель не заведён». Учителя возвращены из 8a8c362. Причину
   стирания по коду не найти, поэтому `save_tables` теперь откладывает копию
   в `data/backups/`, если запись опустошает непустую таблицу. Уехало в `main`.
2. **Анонимизация в Streamlit** — переключатель на экране результата. В `main`.
3. **`server/`** — FastAPI: школы с ревизиями в SQLite (каждое сохранение — новая
   ревизия, стереть молча нельзя), солвер в отдельном процессе, ход по SSE,
   `/validate`, `/heatmap`, `/move`, выгрузка Excel. Интерфейс отдаётся тем же
   сервером. `Dockerfile` — один образ.
4. **`web/`** — React + Vite + Tailwind: школы, составление с живыми цифрами,
   сетка расписания с ручной правкой.
5. **`lad/explain.py`** — объяснимость ручной правки. Ход — это ОБМЕН уроков,
   а не перенос: при переносе 39 клеток из 40 выходили «нельзя» (сетка класса
   плотная), при обмене для того же урока 5 «можно», 4 «хуже», 31 «нельзя»,
   у каждой клетки причина словами. Подсветка недели — ~0,5 с.
6. **Дизайн-основа** — `docs/DESIGN.md` (палитра-тетрадь, цвета `ok/worse/no` =
   `Verdict.level`, IBM Plex Sans из пакета, шапка сверху, словарь, чек-лист),
   кирпичи `web/src/ui/`. ⚠️ IBM Plex Sans Condensed без кириллицы — узкое
   начертание берём осью ширины переменного Plex Sans.
7. **Раздел «Данные»** (из дня 3, раньше плана) — `server/entry.py` поверх
   `lad.tables`: классы по параллелям, кабинеты с «хватает ли», предметы
   и черновик нагрузки по плану № 75, назначение учителей по предмету, раздача
   поровну, учителя вставкой из Excel, пожелания кликом. Автосохранение ревизиями.
8. **Замены** (из дня 4) — `lad/substitute.py` + страница с листом на печать.
   Кандидат должен быть свободен и мочь работать; порядок: ведёт предмет → знает
   класс → уже в школе без нового окна. Методдень, совместитель, потолок — в «чем хуже».
9. **Найдена тихая потеря уроков**: строка нагрузки без учителя молча выпадала
   из расписания, проверка говорила «всё в порядке». Теперь это проблема ввода.
10. Пустая школа на «Составлении» ведёт во ввод данных, а не к мёртвой кнопке.

### Надёжность солвера — ЗАКРЫТО 14.09

Первое законное расписание на школе завуча (24 класса, 838 ч), нормы запретами:
**185 с / не нашёл за 300 с / 86 с**. Отсюда «не успел» у завуча.

| Вариант | Первая сетка | Нарушений норм за 300 с |
|---|---|---|
| Нормы запретами (было) | 86–300+ с | 0, если нашёл (2 из 3) |
| Нормы штрафом ×1000 | 1,2 с | 29 |
| Черновик «только нормы» → подсказкой в запреты | 1,1 с | 36 / 28 / 0 |
| + дни физкультуры явно (3 ч при пятидневке → пн/ср/пт), бюджет 120 с | 2,2–34 с | 1 / 0 / 18 |
| + черновику 80% бюджета, доводке потолок «нарушений не больше, чем в черновике», 120 с | **1,6–2,8 с** | **0 / 0 / 0 / 0 / 0** (окон у учителей 59–71) |

**Диагноз (черновик «только нормы», 120 с, 3 сида):** оставались ТОЛЬКО нарушения
«физкультура два дня подряд», по 2–3 класса; остальные нормы — ноль за секунды.
Причина — упаковка: у всех 24 классов по 3 ч физкультуры, без соседних дней это
только пн/ср/пт, а залы вмещают 3 класса в час → 72 урока на 72 места.
Явная фиксация дней помогла (два прогона из трёх 0–1); третий добил потолок нарушений в доводке.

✅ **ЗАКРЫТО 14.09.2026:** 5 прогонов из 5 — ноль нарушений норм за 120 с (критерий
плана был «за ≤ 5 минут»). Первая сетка за 1,6–2,8 с, окон у учителей 59–71
против 100–157 раньше. В `dev`. В `main` (Streamlit) — только по команде Влада.
Прогон на других школах (та же версия):

| Школа | Бюджет | Первая сетка | Нарушений норм | Окон у учителей |
|---|---|---|---|---|
| Завуча, 24 кл., 838 ч | 120 с × 5 | 1,6–2,8 с | 0 ×5 | 59–71 |
| Гимназия `school_big`, 28 кл., 980 ч | 120 с × 3 | 3,0 с | 2 ×3 | 75–104 |
| Город `demo_gorod`, 28 кл. | 60 с | 1,5 с | 2 | 109 |
| Район `demo_raion`, 490 ч | 60 с | 0,6 с | 0 | 22 |
| Село `demo_selo`, 236 ч | 60 с | 0,3 с | 0 | 3 |

Одинаковые «2» на 28 классах — **доказанный минимум, а не сбой**: у 11А 4 ч
физкультуры, без соседних дней в пятидневке их не расставить. Черновик за 7–8 с
возвращает OPTIMAL с 2 нарушениями, оба «у 11А физкультура два дня подряд»;
солвер сам ослабил норму только для 11А и советует перенести час в шестой день.
Экран расписания теперь показывает эти пояснения (`meta.relaxed`).

### Ещё сделано 14.09

- **Кабинеты не «прыгают»** — `assign_rooms`: у класса свой обычный кабинет, нестрогий
  предмет при занятом спецкабинете идёт в кабинет своего класса. На примере
  кабинетов у класса за неделю (все типы): медиана 12 → 5, максимум 17 → 6;
  уроков без комнаты и конфликтов по-прежнему 0. В `dev`.
- **Печать A4** по классам и по учителям (`/s/:id/print`), ФИО можно скрыть.
- **Excel данных школы** — «Скачать в Excel» / «Загрузить из Excel» на странице данных
  (листы = таблицы ввода, разбор терпимый, отчёт о нераспознанном). Найдено на круге
  «скачал → загрузил»: пустая клетка = NaN → «nan» → каждый урок целым классом
  становился подгруппой «nan». Пустые текстовые значения теперь "" в `tables_from_dict`.

- **Сквозной прогон в браузере** — `tests/e2e_path.py` (Playwright): новая школа →
  данные через интерфейс → составление → ручной ход → замены → печать → Excel.
  Проходит целиком за ~19 с, без ошибок консоли и ответов ≥ 400. Поймал: мастер
  кабинетов предлагал спецкабинеты «по одному на глаз» → путь «по порядку» давал
  нерешаемую школу (0 кабинетов обсл. труда, 1 компьютерный на две подгруппы).
  Теперь мастер считает фонд по классам и плану (`rooms/suggest` → `demo_city._rooms_for`).

- **Первый экран** — фрагмент сетки в момент правки (выбранный урок, подсветка,
  заметка «Сюда нельзя» с п. 94 ССЭТ) и путь из четырёх шагов. Пример обезличен.
- **Сценарий показа 19.09** — `docs/DEMO.md`: маршрут на 5–7 минут, цифры из замеров,
  один вопрос (§8.6), честные границы (§8.5), проверка накануне.

### Прокачка алгоритма — стенд `bench/solver.py` (14.09, школа завуча, 120 с, сиды 1–3, медианы)

| Вариант | До нуля норм | Окна | 2ч в соседние дни | 3ч подряд | День ради урока | Дни ±3 | Пн-пик |
|---|---|---|---|---|---|---|---|
| база | 27 с | 66 | 43 | 13 | 27 | 16 | 5 |
| `draft_comfort` | **не дошёл** (7–18 нарушений) | 141 | — | — | — | — | — |
| `pe_decisions_first` | 44 с, первая сетка 7 с | 86 | — | — | — | — | — |
| логика: spacing 6 + single 5 + balance ×4 + peak ×3 | 26 с | 75 | **27** | 11 | **21** | 15 | 5 |

- `draft_comfort` и `pe_decisions_first` — отклонены (хуже базы).
- Разнесённость предметов и штраф «день ради одного урока» — работают.
- Множители ровности дней и пика — эффекта нет. Причина, вероятно, в залах:
  физкультура в пн/ср/пт стоит на всех 8 уроках (по 9 на номер) при 72 уроках на
  72 места; класс без окон, значит 9 классов в эти дни учатся до 8-го урока.
  Неровные дни навязаны нехваткой спортзала — это находка для завуча, весами её не снять.
- Хвост «1 → 0» во всех прогонах держит «физкультура первым или последним уроком».
| logic2: spacing 6 + single 5 (**включено по умолчанию**) | 21 с | 67 | **31** | 14 | **18** | 18 | 3 |

Гимназия (28 кл., 2 сида): база → logic2 — нарушений 2 → 2 (доказанный минимум, 11А),
окна 69 → 81 (сиды 70 и 92), 2ч в соседние дни 58 → 48, 3ч подряд 12 → 9, день ради урока 34 → 26.

**Рычаги спортзала (15.09, школа из примера, 120 с, 3 сида, медианы; база = logic2):**

| Вариант | До нуля норм | Нарушений | Окна | Дни ±3 | Разброс дней | 2ч подряд | День ради урока |
|---|---|---|---|---|---|---|---|
| база (зал 2+1 места, физкультура не подряд — жёстко) | 21 с | 0 | 67 | 18 | 64 | 31 | 18 |
| физкультура подряд — **мягко** | не дошёл | **3–4** | **185** | 17 | 65 | 43 | 23 |
| большой зал **на 3 класса** (3+1 места) | **5,5 с** | 0 | **55** | **10** | **55** | 32 | 18 |

- «Мягко» для физкультуры подряд — вредно: солвер теряет подсказку «пн/ср/пт», а каждый
  случай подряд всё равно нарушение. Кнопку «разрешить два дня подряд» из карточки
  спортзалов убрали; при включённом «мягко» карточка честно пишет, что стало хуже.
- Вместимость зала — рабочий рычаг: неровных дней почти вдвое меньше, нормы в 4 раза быстрее.
  Это совет завучу: если в зале реально могут заниматься 3 класса — указать это.
- Предпочтения школы (12 ползунков) — на «Составлении». Проверка новых штрафов
  (90 с, 1 сид, короткая пятница + трудные не с 6-го + меньше сдвоенных + физкультура
  не первым): трудные с 6-го урока **62 → 33** (работает); пятница 6,38 → 6,46 урока,
  физкультура первым 9 → 9, сдвоенных 3 → 5 — не сработали по арифметике школы
  (пятница — день физкультуры при забитых залах; 9 уроков физкультуры обязаны
  стоять первым; 6-часовой предмет в пятидневку требует сдвоенного). Ползунки
  теперь пишут это прямо у себя.

- На экране «Составление» — загрузка спортзалов и строгость каждой нормы
  (жёстко/мягко/не учитывать, сохраняется в settings.rules школы).

### Закрепление уроков и пересборка (15.09)

На экране расписания: «Закрепить» у выбранного урока (несколько сразу, подгруппы —
вместе), панель закреплённых, «Пересобрать вокруг закреплённых», а при щелчке по
красной клетке — «Поставить сюда и пересобрать остальное». Ход пересборки виден,
по завершении переставленные уроки перелетают в новые клетки, клетки вспыхивают,
сводка «переставлено N, окна A → B». «Отменить ход» возвращает сетку до пересборки.

Солвер: `solve(hint=…)` — старт с текущей сетки; `stay=…` — штраф за каждый урок,
ушедший со своего места (TUNING["stay_weight"] = 6, в черновике — вторичный критерий).
Замер (сетка 60 с, один урок закреплён в «красной» клетке, пересборка 40 с):

| | Переставлено | Закреплённый на месте | Нарушений | Окна |
|---|---|---|---|---|
| без штрафа | 733 из 838 | да | 1 | 204 |
| со штрафом | **54** из 838 | да | **0** | **164** |

**«Остановить» теряло сетку** (найдено тем же прогоном): should_stop слушал только колбэк
найденного решения; в фазе «первая сетка» его нет, через 10 с сервер убивал процесс —
«процесс завершился без ответа». Теперь поток-наблюдатель зовёт `stop_search()` в любой
фазе, запас до принудительного завершения — 30 с. Проверка: стоп на 1,5 с — ответ через
2,1 с с полной сеткой (838 уроков); стоп на 12 с — через 0,1 с.

Найдено сквозным прогоном: после щелчка курсор остаётся над клеткой, и превью
наведения перекрывало карточку отказа вместе с кнопкой пересборки.

### «Что если» (15.09)

Вкладка «Что если»: одно изменение — учитель не может в день, в зале на класс больше,
другой режим, норма мягче. Считаются **два варианта одновременно и одинаково** (тот же
старт, бюджет, ядра поровну): без изменения и с ним. Сравнивать с сохранённым расписанием
было бы нечестно — разницу давал бы лишний поиск (§8.3). Таблица «без / с / разница»
(±10% помечено «≈» — шум поиска), неделя класса или учителя рядом с подсветкой отличий,
«Принять» пишет изменение в данные школы и делает вариант текущим расписанием.
Сравнение живёт в адресе (`?control=&variant=`), переживает перезагрузку. Варианты
лежат в `schedules` с `meta.change` и в «последнее расписание» не попадают.
Код — `server/whatif.py`, `web/src/pages/WhatIfPage.tsx`.

Замеры (школа из примера):
- «Иванюк не может во вторник», от текущего, 60 с: окна 67 → 77, переставлено 14 уроков,
  нормы 0; после «Принять» у учителя во вторник 0 уроков.
- «Зал на класс больше» **от текущего** — переставлен 1 урок, эффекта не видно: штраф за
  перестановку держит сетку. Поэтому для зала, режима и норм по умолчанию «С нуля».
- «Абрамчук не может в среду» — солвер ответил «решения нет» за 1 с без причины: у учителя
  математика по 5 ч, без сдвоенных это урок каждый день. Теперь предпроверка
  (`blockers`) отвечает сразу и с пунктом п. 65 ССЭТ; то же для физкультуры пн/ср/пт (п. 94).

На экране расписания — карточка «Логика расписания» (`lad/quality.py` в отчёте:
неровные дни, пик в понедельник, предметы в соседние дни, выходы ради одного урока).

Проверка в браузере: форма → нельзя сразу с причинами → расчёт → сравнение →
перезагрузка → «Принять» — проходит, ошибок в консоли нет.

### Сетка расписания: прямое управление (15.09)

- **Подсветка учителя** — наведение на фамилию подсвечивает все его уроки по сетке,
  справа карточка: уроков, дней, окон, уроки по дням.
- **Поиск ⌘K** (кнопка в шапке) — раздел, класс, учитель. Класс — колонка прокручивается
  и подсвечивается; учитель — подсветка остаётся до Esc.
- **Клавиши** — стрелки двигают курсор, Enter выбирает урок и ставит его в клетку, Esc
  снимает всё, ⌘Z отменяет ход. Подсказка — в карточке «Как поправить руками».
- **Трудность по дням** (переключатель, помнится) — классы × дни, сумма баллов, «!» у
  классов, где пик не во вт/ср/пт (п. 94). На примере — 5 таких классов.
- **Перетаскивание** мышью — тот же ход, что щелчком; призрак цвета клетки под курсором,
  у края сетка прокручивается сама. Касание пальцем — по-прежнему щелчками.

Найдено прогоном в браузере: поиск склеивал новую строку с прошлой («7бИванюк» — сброс
приходил позже первых нажатий); своя клетка урока красилась зелёным, и призрак обещал
ход, которого не будет; варианты хода лежали ниже экрана — без автопрокрутки не дотащить.

### Листы файлом (15.09)

- **Лист замен**: «Скачать PDF», «Скачать Excel», «Скопировать для чата» (текстом, по строке на
  урок), «Отправить» (PDF через меню «Поделиться» — только где браузер умеет делиться файлом:
  телефон, Safari), «Распечатать». Все выходы собираются из одних строк — в файле ровно то,
  что выбрано на экране, со скрытыми ФИО, если их скрыли.
- **Печать расписания**: «Скачать PDF» — все листы A4 одним файлом (24 по классам — 73 КБ,
  52 по учителям — 100 КБ, меньше секунды), «Скачать Excel» — книга расписания.
- PDF собирает сервер: `lad/sheets.py` на fpdf2 и IBM Plex Sans из `data/fonts` (кириллица,
  лицензия OFL рядом). fpdf2 добавлен в `requirements.txt` и `Dockerfile`.
- «Отсутствует: Бондарь С. Л..» — точка ФИО удваивалась точкой предложения. Исправлено.

### Версии расписания — открыть готовое и работать (15.09)

Сохранялось всё (каждое составление, пересборка, правка — строкой в `schedules`), но открыть
можно было только последнее. Теперь:
- **«Ваши школы»**: у школы с расписанием — «Расписание от сегодня, 14:05» и кнопка
  «Открыть расписание» прямо в сетку; без расписания — «Продолжить ввод».
- **«Версии (N)»** на экране расписания: откуда версия (составлено, пересборка, ручная правка,
  принято из «Что если», возврат), название, когда, нарушения норм и окна; «по прежним данным
  школы», если данные менялись. Цифры версии считаются один раз по данным ТОЙ ревизии и
  кешируются в meta.
- Открыть любую (`?version=`) → баннер «Открыта версия от…» с «Сделать эту версию текущей»
  (копия встаёт последней, история не теряется) и «Открыть текущую».
- **«Сохранить версию»** и ⌘S — с названием по желанию.
- Несохранённые ходы: браузер переспрашивает перед уходом; другую версию не открыть, пока
  ходы не сохранены или не отменены. Esc и подсветка учителя больше не сбрасывают `?version`.

Проверка в браузере (копия примера с двумя версиями): открыть из списка школ → панель версий →
прежняя → Esc → сделать текущей → ход → ⌘S с названием → перезагрузка — проходит.

### Excel данных школы: понятно, как оформить (15.09)

- Одно описание колонок — `lad/data_excel.py` (SPEC): из него лист «Как заполнять» в файле,
  подсказки у заголовков, выпадающие списки, панель «Как оформить Excel» на экране данных
  и разбор при загрузке.
- В файле: первый лист «Как заполнять», синие заголовки — обязательные, «да/нет» вместо
  True/False, методический день «Пн…Пт». Кнопка «Скачать пустой шаблон».
- Загрузка понимает «да/нет/+/-», «среда/Ср/Пятн.», «Спортзал» в любом регистре, «5,0»,
  регистр и пробелы в названиях листов. Непонятое — в отчёт с листом, строкой, колонкой
  и тем, что писать; значение не угадывается.
- Найдено: «нет» в колонке да/нет читалось как «да»; «Ср» в методическом дне пропадал;
  пустое «только в нём» давало «нет», и физкультура из файла могла уйти в обычный класс —
  теперь пусто = решает система по типу кабинета.
- Проверка: круг «скачал → загрузил» на примере — 560 строк, 0 отличий; «человеческий»
  файл — верные значения и 4 внятные ошибки; панель и отчёт в браузере.

### Журнал замен по датам (15.09)

- «Сохранить в журнал» у листа замен. Запись — один отсутствующий учитель в одну дату;
  повторное сохранение обновляет её. Храним ИМЕНА, не id: id учителя — номер строки
  и сдвигается при правке списка, а журнал за сентябрь должен читаться и в ноябре.
- Повторный подбор той же даты и учителя берёт выбор из журнала, а не заново «лучшего».
- Раздел «Журнал замен»: месяц, записи по дням («Открыть», «Удалить» в два щелчка),
  итог «кто сколько заменял», «Скачать Excel за месяц» (листы «Замены», «Кто заменял»,
  «Кто отсутствовал») — для учёта часов замен.
- Код: `server/journal.py`, таблица `substitutions` в `server/db.py`,
  `web/src/pages/SubstitutionJournal.tsx`.

### Импорт своей таблицы нагрузки (15.09)

«Данные» → «Импорт своей таблицы нагрузки». Не «загрузите по форме», а «покажите, какая
колонка что значит»: система угадывает строку заголовков, раскладку и колонки, человек
правит, предпросмотр пересчитывается сразу.
- Раскладки: «учитель × классы» (тарификационный список: классы колонками, в ячейке часы)
  и «строка на урок».
- Разбирается и попадает в отчёт: объединённые ячейки (ФИО на несколько строк), «5 «А»»,
  «5-а», «5A» латиницей, ФИО полностью → «Фамилия И. О.», «Матем.», «Рус. яз.», «Физ-ра»,
  «Англ. яз.» → названия плана № 75, строки «Итого», колонка «Всего»; один предмет в классе
  у двух учителей → подгруппы 1 и 2. Не понятое («1/2», «полчаса», «12А») — со строкой Excel.
- «Заменить нагрузку» или «Добавить к нагрузке»; недостающие классы, предметы (кабинет
  и «только в нём» — по плану) и учителя заводятся.
- Код: `lad/tarif_import.py`, `server/tarif.py`, `web/src/pages/data/TarifImport.tsx`.
- Проверка: два файла разной раскладки — разбор верный; в браузере догадка, смена строки
  заголовков, загрузка «Заменить» (12 строк) и «Добавить» (14).

### Режим показа и новый сценарий демо (15.09)

- Главная → «Режим показа»: чистая копия примера (с готовой сеткой-заготовкой, если пример
  уже составляли) и полоса внизу: «3 из 10, Составление», «Назад»/«Дальше», PageDown/PageUp
  (кликер), «Подсказка» (H) — что сделать и что сказать, залу не видна, пока не открыть.
  Маршрут — `web/src/show.ts`, полоса — `web/src/ShowBar.tsx`.
- `docs/DEMO.md` переписан под 10 шагов: проблема → данные и импорт тарификации →
  составление → «почему нельзя» и перетаскивание → «поставить силой» → логика расписания →
  «что если» → замены и журнал → PDF → вопрос. Всё локально, деплой — последним этапом.
- **Прогон с таймером (Playwright, все 10 шагов, ключевое действие на каждом):** проходит,
  ошибок нет. Ожидания системы: ноль нарушений через 19–26 с после первой сетки,
  пересборка вокруг закреплённого — 41 с (переставлено 103, окна 251 → 181, нормы соблюдены).
  С речью по таймингам сценария — около 6 минут.
- **Найдено прогоном:** остановка составления ровно на 20-й секунде дала в сетке
  1 нарушение норм и 280 окон — красная строка на весь экран перед залом. Подсказка шага
  и сценарий теперь говорят: останавливать, только когда «Нарушений норм» = 0.

### Мелочи, которые мешали (16.09)

- **Норма — первой причиной отказа.** «Нельзя» с пунктом ССЭТ теперь стоит выше совпадения
  учителей: именно им завуч отвечает учителю. Проверено на 1032 отказах примера — в 476
  причины смешанные, норма не первой ни разу (в прогоне показа 15.09 было наоборот).
- **Старый .xls в импорте нагрузки.** Тарификация у школ часто лежит в формате 97–2003;
  теперь читается вместе с объединёнными ячейками (xlrd). «Загрузить из Excel» тоже принимает
  .xls. Проверено на собранном .xls: 7 строк, ФИО из объединённой ячейки, «Рус. яз.» узнан.
- **«Что проверить» после импорта.** Список того, что система заполнила догадкой, с кнопкой
  «Открыть шаг»: нет кабинетов, кабинет предмета «наугад», новые учителя без методического
  дня, новые классы с 24 учениками, деление по двум учителям, строки без учителя, пропущенные.
- **Удаление школы** из списка в два щелчка — со всеми ревизиями, расписаниями и журналом
  замен (`DELETE /api/schools/{id}`). Тестовые копии больше не копятся навсегда.

### Пересборка не ждёт весь бюджет (16.09)

`solve(settle=…)`: этап доводки останавливается сам, если нормы на нуле и нет ЗАМЕТНОГО
(≥ 1% штрафа) улучшения `settle` секунд. Пересборка вокруг закреплённых уроков шлёт
`settle = 8`. Замер (школа из примера, 2 сида, урок закреплён в «красной» клетке):

| | Время | Переставлено | Нарушений | Окна |
|---|---|---|---|---|
| без остановки | 40 и 40 с | 42, 47 | 0 | 136, 123 |
| settle = 5 | 14 и 30 с | 25, 51 | 0 | 138, 138 |
| **settle = 8** | **31 и 28 с** | 46, 29 | **0** | 134, 129 |

**Замеры поймали две настоящие ошибки, обе старше этой правки:**
1. **Потолок нарушений для доводки брался из штрафа черновика.** При пересборке штраф —
   смесь «нарушения × вес» и «сколько уроков сдвинулось», потолок выходил огромным, и
   доводка имела право наставить нарушений: 43 нарушения и 711 переставленных уроков.
   Теперь потолок — реальное число нарушений в черновике, посчитанное валидатором.
2. **Черновик нельзя останавливать по затишью.** Его счётчик нарушений модельный и
   расходится с валидатором: остановка ловила момент «модель думает, что ноль», и в сетке
   оставалось 25 нарушений. Ранняя остановка теперь только у доводки, у которой есть потолок.

Третий замер показал, что без порога заметного улучшения затишья не наступает вовсе:
доводка непрерывно улучшает штраф на единицы, и все прогоны доигрывали 40 с.

Через приложение (API, та же школа): 40,8 с без остановки против 18,2 с с `settle = 8`.
В прогоне показа пересборка всё же заняла 41 с — и это правильно: стартовая сетка там была
плохой (окна 252 → 178), доводка всё время находила крупные улучшения, останавливаться
было не на чем.

### Саммари проекта одним PDF (16.09)

Друг Влада говорит за проект в ПВТ и просит саммари: он закидывает его в чат, дальше синк
с министерством (по его оценке — 30–40 дней согласований). Нужен лист, который переживает
пересылку дальше по цепочке, без конкретного адресата.

- `docs/ONEPAGER.md` — текст: задача, что делает ЛАД, измеренное, честные границы, просьба
  (имя человека, а не бюджет — §8.6).
- `tools/summary_shots.py` — экраны с локального сервера в `data/summary/`.
- `tools/summary_pdf.py` — сам лист: `.venv/bin/python tools/summary_pdf.py out/lad-summary.pdf`.
  Три страницы: текст, сетка с подсветкой и причиной нормы, замены. Подпись — @vladneki.

**Найден дефект обезличивания.** При «Скрыть ФИО учителей» сетка показывает «Учитель N»,
а панель «Сюда нельзя» всё равно печатала фамилию: причины приходят с сервера готовой
строкой (`validate.py` подставляет `Teacher.name`). На экране показа и в PDF это прямое
нарушение §8.4. Фронт теперь обезличивает и текст причин (`mask` в `SchedulePage.tsx`,
применяется к blocking/costs/gains и к подписям вариантов).

Экраны сперва подгонялись под один лист автоматически — сетка становилась нечитаемой.
Оставлена полная ширина: лишняя страница дешевле, чем нечитаемое расписание.

**Расширение листа (тот же день).** Добавлены разделы «Почему это непросто» (NP-трудность,
два прохода солвера и почему нормы запретами не работали, ранняя остановка, обмен вместо
переноса, один валидатор и равные бюджеты в «Что если»), «Чем это отличается от существующих
систем» (солвер есть у всех — Untis, aSc, «Ректор»; норм РБ из коробки нет ни у кого; ввод
данных не автоматизировал никто; schools.by и «Электронная школа» расписание только хранят)
и «Что меняется в школе» по ролям. Экранов стало семь: сетка, подсветка с причиной,
трудность по дням, ввод данных, «Что если», замены, листы на печать. Итого 6 страниц.

**Обезличивание стало общим (16.09).** Было три независимых чекбокса «Скрыть ФИО»
(расписание, замены, печать), а «Что если» показывал фамилии всегда — на показе
посторонним достаточно было перейти в соседний раздел. Теперь это один переключатель
в шапке: `web/src/hideNames.ts` (состояние в sessionStorage + `masker`/`alias` для текстов,
приходящих с сервера одной строкой), чекбокс в `Shell.tsx`, страницы расписания, замен,
печати и «Что если» подключены к нему. Дубли чекбоксов со страниц убраны; на печати
переключатель остался — она открывается вне общей шапки.

Раздел «Данные» намеренно не обезличивается: там завуч вводит своих учителей и должен
их видеть. Для листа кадр берётся с шага «Классы».

**Лист расширен до 7 страниц.** Добавлены разделы «Почему это непросто», «Чем это
отличается от существующих систем», «Что меняется в школе» и девять экранов, включая
живые: солвер на ходу с тремя этапами и подобранные замены с обоснованиями.
`tools/sample_tarif.py` делает пример чужой тарификации (пока в листе не используется).

### Замечено, не сделано

- Лист печати берёт последнюю СОХРАНЁННУЮ версию; сохранения замен по датам нет.

### Ждёт Влада

Порядок на время ожидания встречи по линии ПВТ (~30–40 дней), от важного к
второстепенному. Ждать встречу, ничего не делая, нельзя: придём на неё с тем
же, с чем сидим сейчас.

1. **Вердикт завуча** «можно ставить в работу» — фильтр 0 по §10, идти в
   госконтур до него нельзя. Бесплатно, доступно на этой неделе, и это
   единственный человек, который скажет «так расписание не ставят».
   Пять вопросов — §8.2 CLAUDE.md.
2. **UNKNOWN примерно в трети запусков** на школе завуча. Если выпадет на
   показе, объяснять экономику будет уже некому. Чинить сейчас, а не в день
   встречи.
3. **Вторая и третья пилотные школы** (гимназия с подгруппами, сельская
   малокомплектная). Входной билет к ГИАЦ — три школы и одна отработанная
   четверть; четверть идёт дольше, чем ожидание встречи, поэтому начинать
   надо сейчас, иначе придём с обещаниями.
4. Отправить саммари другу для чата ПВТ (`out/lad-summary.pdf` пересобирается
   из `tools/`), экономику — Стасу (`docs/summary-for-stas.md`).

**Чего сознательно НЕ делаем до встречи:** юрлицо, ПВТ, наём, Railway,
полировка интерфейса. Это либо тратит деньги, либо съедает те же три месяца.

---

## Где мы были (08.09.2026)

Система работает целиком: ввод данных → составление → проверка → выдача
(HTML и Excel). Развёрнута на **https://nekiae-lad-timetable.streamlit.app**,
пересобирается автоматически при пуше в `main` репозитория `nekiae/lad-timetable`.

**Ближайшая дата — Startup Day БГУ, ~15.09.2026**, там будут люди из
Министерства образования. Это первый публичный показ; §8.6 CLAUDE.md
(«не питчить, спросить имя») применяется именно к нему.

### Что изменилось относительно исходного плана

**Колонки «было» не будет.** Решено 06.09.2026: система принимает вводные
(классы, кабинеты, предметы, учителя, нагрузка, пожелания) и составляет
расписание. Существующее расписание школы в неё не загружается. Это отменяет
демо-сценарий §6 CLAUDE.md с тремя цифрами «было/стало» и §8.3 про сравнение
одним валидатором. Механика `render(before=...)` в коде осталась, входа для неё
не строим. Ценность показывается цифрами самого результата.

**Ввод стал процессом, а не девятью вкладками.** Завуч идёт по шагам, лента
шагов вверху показывает, где он и что осталось. Данные сохраняются на диск при
каждом изменении, а не по кнопке: ввод школы — это часы работы, и терять их
на закрытой вкладке недопустимо (500 строк сериализуются за 1,6 мс).

**Стартовые данные — школа завуча**, Жемчужненская СШ Барановичского района:
24 класса V–XI, 52 учителя, 428 строк нагрузки, 838 часов. Приложение
открывается сразу на ней, а не на пустом файле и не на гимназии.

---

## Сделано в эту сессию (08.09.2026)

### 1. Фаза улучшения доводится до конца бюджета

CP-SAT иногда завершает поиск сам, задолго до лимита, со статусом FEASIBLE —
то есть без доказательства оптимальности. Замерено 07.09.2026 на гимназии
(28 классов), бюджет 300 с, одни и те же данные:

| Прогон | Вторая фаза | Окон у учителей |
|---|---|---|
| А | 291 с из 291 | 38 |
| Б | 166 с из 294 | 52 |
| В | 25 с из 279 | **268** |

Разброс в семь раз при одинаковом вводе. Для показа это хуже, чем медленно:
нельзя нажать кнопку и не знать, что покажет экран.

Исправлено в `src/lad/solve.py`: после каждого возврата солвера, если время
ещё осталось, поиск перезапускается от лучшего найденного (старые подсказки
снимаются через `ClearHints` — повторный `AddHint` для той же переменной делает
модель невалидной). Лучшее решение держится отдельно: следующий круг стартует
с той же точки, но пойти может хуже, и наружу отдаётся не последнее, а лучшее.
Выход из цикла — доказанный OPTIMAL, исчерпанный бюджет, просьба остановиться,
мгновенный возврат солвера (значит, следующий круг будет таким же)
или страховочный предел в 8 кругов.

Замер после правки, гимназия, бюджет 300 с — см. таблицу в разделе «Замеры».

### 2. Экран результата: три главные цифры

Валидатор даёт восемь величин, и все восемь одного размера читались как
таблица — глаз не знает, с чего начать. На показе третьим лицам времени
разбираться нет ни у кого.

Наверх вынесены три цифры, ради которых всё делалось: **за сколько составлено,
сколько нарушено санитарных норм, сколько у учителей окон.** Крупно, антиквой,
в одной карточке, с поясняющей строкой под каждой («980 уроков, 28 классов»,
«на 60 человек за неделю»). Остальные пять величин — обычными метриками ниже.
Статус солвера ушёл в подпись мелким шрифтом и переведён на человеческий:
OPTIMAL → «доказано, что лучше не составить», FEASIBLE → «все требования
соблюдены; на доказательство того, что лучше нельзя, времени не хватило».

Функция `headline()` — в `src/lad/style.py`, на узком экране колонки
схлопываются в столбец (цифра в 3rem в трети телефонного экрана переносится
по цифрам).

### 3. Стартовые данные не решались — поправлено

Прогон школы завуча со всеми нормами в жёстком режиме давал **INFEASIBLE
за 25–35 секунд**. То есть завуч открывал приложение, нажимал кнопку
и получал «расписания нет».

Разбор (`diagnose`) находит причину за 43 с и называет её точно:

> Кабинетов не хватает. Всего 30 на 24 класса. Класс учится без окон, поэтому
> на первом уроке заняты все классы сразу. Самые тесные: «спортзал» — 1 шт.
> на 72 ч, «физика» — 1 шт. на 40 ч, «биология» — 1 шт. на 34 ч.

Это дефект **данных, а не кода**: в файле 22 обычных кабинета на 24 класса
и один спортзал на 72 часа физкультуры в неделю. Диагностика при этом
отработала ровно так, как задумана, — назвала причину и что делать.

**Исправлено по решению Влада 08.09.2026** — вариант «поставить правдоподобный
минимум и пометить файл как пример»: обычных кабинетов стало 24 (по одному
на класс), добавлен малый спортзал. Реальный фонд школы по-прежнему неизвестен
и остаётся открытым вопросом № 3. Цифры и следствия — в разделе «Замеры».

---

## Замеры

### Гимназия (28 классов, 60 учителей, 43 кабинета, 980 часов), бюджет 300 с

Все нормы в жёстком режиме, веса по умолчанию.

| Прогон | Итог | Время | Штраф | Окон у учителей | Норм | Конфликтов |
|---|---|---|---|---|---|---|
| 1 | FEASIBLE | 300 с | 3164 | **29** | 2 | 0 |
| 2 | FEASIBLE | 300 с | 3313 | **34** | 2 | 0 |
| 3 | FEASIBLE | 300 с | 2920 | **29** | 2 | 0 |
| 4 | FEASIBLE | 300 с | 2858 | **30** | 3 | 0 |

980 уроков в каждом прогоне, 0 окон у классов. Нарушения — та самая
физкультура в 11А, о которой система предупреждает до запуска.

**Что изменилось.** Было 38 / 52 / 268 окон, стало 29 / 34 / 29 / 30. Разброс
с семикратного упал до 1,2-кратного, и худший результат теперь лучше прежнего
лучшего. Все четыре прогона выработали бюджет целиком (300 с из 300) — до
правки солвер выходил сам на 25-й секунде и отдавал, что успел. Статус
FEASIBLE, а не OPTIMAL: за пять минут доказать оптимальность на 28 классах
не выходит, но расписание законно и хорошо.

### Школа завуча (24 класса, 52 учителя, 33 кабинета, 838 часов)

После правки кабинетного фонда (см. ниже). Все нормы жёстко, веса по умолчанию.

| Бюджет | Прогонов | Нашлось | Окон у учителей | Норм |
|---|---|---|---|---|
| 300 с | 4 | **1** | 50 | 0 |
| 600 с | 3 | **2** | 36 и 47 | 0 |

Каждое найденное расписание чистое: 838 уроков, 0 нарушений норм, 0 окон
у классов, 0 конфликтов. Проблема не в качестве, а в том, что солвер
не всегда успевает: на пяти минутах он находил решение в одном случае
из четырёх, на десяти — в двух из трёх.

**Отсюда бюджет по умолчанию поднят с 5 до 10 минут.** Пять минут остались
в списке с подписью «если школа небольшая». UNKNOWN на десяти минутах всё ещё
случается примерно в трети запусков; в этом случае экран даёт кнопку «искать
дольше» (удваивает бюджет), и это работает — но осадок остаётся, см.
«Следующие шаги».

**Кабинетный фонд поправлен** (08.09.2026): обычных было 22 на 24 класса,
стало 24 — по одному на класс, потому что на первом уроке заняты все классы
сразу. Добавлен малый спортзал: 24 класса по 3 часа физкультуры дают 72 урока
в неделю, а один зал на два класса вмещает 80 при 40 слотах — загрузка 90%,
и это при норме, запрещающей физкультуру два дня подряд и на краю дня.
⚠️ Реальный фонд Жемчужненской школы неизвестен, цифры — правдоподобная
оценка; файл помечен как пример. Спросить у завуча (открытый вопрос № 3).

### Ранее (25–26.08.2026) — три модельные школы

Генератор — `lad.demo_city`, пересборка — `tools/make_demo_schools.py`.

| Школа | Классов | Учителей | Часов | Что проверяет |
|---|---|---|---|---|
| Дубровка (сельская) | 7 | 16 | 236 | учителя-многопредметники, тесный кабинетный фонд |
| СШ № 3 (районная) | 14 | 39 | 490 | методические дни, профили X–XI |
| Гимназия № 7 (городская) | 28 | 60 | 980 | масштаб, четыре профиля, 10 методических дней |

Итог по гимназии на бюджете 10 минут: 980 уроков, 0 конфликтов, 0 окон
у классов, 40 окон у учителей на 60 человек, 3 нарушения — все три физкультура
в 11А, о которой система предупреждает до запуска. Результат проверен
независимым аудитом: `tools/audit_schedule.py` разбирает готовый HTML
и пересчитывает всё с нуля, не используя `lad.validate`.

**Воспроизводимость (до правки фазы улучшения).** Пять прогонов одних данных:
UNKNOWN 150 с (машина занята), OPTIMAL 123 с, 10,6 с, 5,8 с, 19,6 с. Солвер
работает в 8–16 потоков, обмен между ними зависит от таймингов машины, поэтому
каждый запуск идёт по дереву своим путём. Время гуляет в двадцать раз,
исход стабилен.

---

## Что уже исправлено (история дефектов)

Каждый следующий дефект вскрывался только на школе другого устройства —
поэтому модельных школ три, а не одна.

1. **Норма применялась ко всей школе разом.** Физкультура «не два дня подряд»
   при пятидневке вмещает максимум 3 занятия, в 11А их четыре — класс нерешаем
   арифметически, и из-за одного класса из 28 приходилось ослаблять норму
   целиком. Теперь послабление точечное, и система сама называет класс, причину
   и выход. На тех же данных: нарушений 32 → 2, окон у учителей 177 → 54.
2. **Бюджет по умолчанию был 30 секунд**, а первое законное расписание
   на 28 классах находится за 119 с. Стало 5 минут, минимум — минута.
3. **«Не успел» показывалось как «решения нет».** UNKNOWN и INFEASIBLE — разные
   ответы, требующие разных действий. Теперь UNKNOWN даёт «искать дольше»,
   INFEASIBLE — разбор причин.
4. **`diagnose()` мог оговорить невиновную норму** — засчитывал в вину любой
   неуспех, включая «не успел». Теперь виновной считается только доказанная
   невозможность.
5. **Один учитель на обе подгруппы принимался молча.** Подгруппы идут в один
   час, значит вести их обязаны разные люди. Ловится до запуска, с указанием
   класса, предмета и имени.
6. **Нехватку кабинетов не находил никто** — гимназия молчала 7 минут
   и отвечала UNKNOWN. Арифметика по неделе этого не видит: 645 часов
   помещаются в 17 кабинетов, но на первом уроке заняты все 28 классов сразу.
   `diagnose()` проверяет фонд экспериментом (`solve(..., ignore_rooms=True)`)
   и называет самые тесные типы за 7 секунд.
7. **Предупреждение о тесной сетке**: класс, занимающий больше 90% слотов,
   помечается до запуска — солверу не остаётся свободы на нормы.

**Урок, который стоит помнить.** Когда гимназия не решалась, разбор указывал
на два узких места сразу, и был сделан вывод, что норма о трудных предметах
конфликтует с профильным учебным планом. Вывод оказался неверным: арифметика
его не подтверждала, а причина была в кабинетах (28 классам нужно 28 обычных
кабинетов, генератор давал 20, считая по средней доле уроков в спецкабинетах —
среднее сходится, а расписание упирается в ПИК). Объяснение звучало убедительно
и было принято без проверки. **Прежде чем обсуждать нормы — считать кабинеты.**

---

## Следующие шаги

| # | Что | Зачем | Когда |
|---|---|---|---|
| 1 | UNKNOWN примерно в трети запусков на школе завуча | завуч ждёт 10 минут и получает «не успел» | **до показа** |
| 2 | Вердикт завуча (§8.2 CLAUDE.md, пять вопросов) | фильтр 0 точки выхода §10 | желательно до 15.09 |
| 3 | Анонимизация в интерфейсе | на Startup Day реальные ФИО учителей показывать нельзя (§8.4) | отложено сознательно 08.09 |
| 4 | Что говорить на Startup Day | §8.6: не питчить, спросить имя того, кому показать | к 15.09 |

**По пункту 1 — что можно сделать**, если UNKNOWN мешает на показе:
- сетка в 8 уроков вместо 7 развязывала гимназию — проверить то же здесь;
- ещё пара кабинетов даёт солверу свободу (но это снова догадка о школе);
- ослабить одну норму до «soft» на время показа — расписание найдётся всегда,
  но с пометкой о нарушении;
- показывать на гимназии, где решение находится стабильно.

---

## Экономика посчитана (17.09)

Вопросы от Стаса (конкуренты, число пилотов, на чём зарабатываем, финмодель,
точка безубыточности) разобраны в `docs/ECONOMICS.md`, расчёт — в
`tools/economics.py`. Допущения живут в коде, а не в голове: меняешь цифру —
пересчитывается вся таблица.

Что выяснилось и меняет картину:

- **В Казахстане автосоставление уже встроено в бесплатный электронный журнал**
  (BilimClass, 6 000+ школ, 85% уроков без замечаний с первого запуска).
  Зарабатывает не расписание, а экосистема вокруг него. Говорить «аналогов нет»
  больше нельзя — проверяется за минуту.
- **В Китае** госплатформа расписание только отображает, а рынок составления
  вырос под смену правил (реформа 新高考). Тайминг важнее технологии — и в РБ
  правила меняются сейчас.
- **Потолок цены — 2250 BYN/год** (50 базовых величин): ниже порога школа
  закупает простой процедурой. Это ограничение процедуры, а не кошелька школы.
- **Точка безубыточности для двоих — 56 школ** при 1500 BYN/год (1,9% страны).
  ФОТ в 5–10к $/мес требует 6–11% всех школ на платной подписке, то есть
  финансируется не подпиской, а контрактом с ГИАЦ.

**Решение 17.09: держим минимальный режим.** Пилоты бесплатные, денег не берём,
юрлица нет — расходы сводятся к серверу и домену, **840 BYN/год ($278)**. ПВТ
пока не нужен: его льготы бьют по ФОТ и прибыли, которых нет, зато он берёт 1%
с выручки и принимает документы по своему расписанию. Вступать — под наём.

Позиционирование вынесено в `docs/POSITIONING.md`: не «SaaS расписания для
школ» (там нас сравнивают с бесплатным и потолок $1,34 млн), а **движок с
местными нормами и объяснимостью** — ядро, слой норм, объяснение отказа.
Тогда бесплатный BilimClass становится подтверждением спроса, ГИАЦ — каналом,
а не конкурентом, и появляется экспортный ответ для ПВТ.

**Школа как плательщик снята с рассмотрения (17.09).** Система образования
бедная, и прецедент уже есть: у schools.by базовые журналы для школ бесплатны,
платят родители. Считаем не «сколько возьмём со школы», а сколько задача стоит
тому, кто может платить. Разговор с ГИАЦ ведём от их расходов: написать модуль
самим — **$72 тыс. и год втроём**, купить готовое — $22–36 тыс. Лицензия на
страну по 100–300 BYN за школу = 7–20% от того, во что ручное составление
обходится сейчас (4,4 млн BYN/год времени завучей, ставка `UNVERIFIED`).

Вводные Влада: горизонт **3 месяца** до вердикта завуча, личный бюджет
**$30/мес**, цель — **контракт или пилот с ГИАЦ**. Три месяца в режиме пилота
стоят 210 BYN при бюджете 272 BYN — укладываемся. Денег в эти три месяца не
берём ни с кого, юрлицо не открываем.

---

## Открытые вопросы

| # | Вопрос | Статус | Почему важно |
|---|---|---|---|
| 1 | Пишет ли ГИАЦ модуль расписания сам? | ⏸ отложено | определяет судьбу проекта (§10); запрос не шлём до вердикта завуча |
| 2 | Санитарные нормы РБ | ✅ добыты 22.08.2026 через web.archive.org, оцифрованы в `data/sanpin_by.json` (`domain.md` §12) |
| 2а | Предельная недельная нагрузка и длительность урока | ✅ закрыт 23.08.2026: типовой план № 75, урок 45 мин (ССЭТ № 525 прил. 12) |
| 2б | Максимум уроков в день | ❗ прямой нормы нет ни в одном документе — проверены СанПиН, ССЭТ с приложениями, типовые планы, ИМП. Выводится косвенно |
| 2в | Расхождение первоисточников по нагрузке | ⚠️ прил. 16 ССЭТ vs типовой план № 75 (IX: 33/34, X–XI: 34/35). Взят план, расхождение в конфиге. **Спросить у завуча, какой цифрой пользуется школа** |
| 3 | Реальный кабинетный фонд школы завуча | ❗ **новое, 08.09.2026** | без него стартовые данные нерешаемы (см. выше) |
| 4 | Входит ли расписание в дорожную карту «Электронной школы»? | ❓ по открытым источникам не видно | сайты edu.gov.by / giac.by не открываются из-за границы |
| 5 | Вердикт завуча: можно ли ставить в работу? | ❓ | фильтр 0 точки выхода (§10) |

Вопрос «есть ли доступ к реальному расписанию школы» **снят** — импорт
расписания не делаем (см. «Что изменилось»).

## Заметки

- Сайты `.by` (edu.gov.by, giac.by, schools.by, adu.by) отдают ECONNREFUSED
  из-за границы. Открывать вручную или искать зеркала.
- Цифры по нормам НЕ выдумывать. Не подтверждено → `UNVERIFIED`.
- После правки модуля в `src/lad/` сервер Streamlit надо **перезапустить**:
  `runOnSave` перечитывает только `app.py`.
- Расписания трёх модельных школ и Excel — в `out/demo/` (не под гитом).
