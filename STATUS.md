# STATUS — рабочий лог проекта ЛАД

Обновлять в конце каждой сессии. Контекст проекта — в `CLAUDE.md`.

---

## Текущая фаза

**День 5 из 7.** Солвер, валидатор, рендер и рабочее место готовы.
Не хватает одного — реальных данных школы (см. «Следующие шаги»).

### Ревизия готовности 25.08.2026 (перед передачей завучу)

Полный прогон на модельной городской школе: 28 классов, 60 учителей,
500 строк нагрузки, 980 часов. Найдено и исправлено четыре дефекта, любой
из которых сорвал бы первый же сеанс работы завуча.

**1. Норма применялась ко всей школе разом.** Физкультуры «не два дня подряд»
(п. 94 ССЭТ) при пятидневке вмещает максимум 3 занятия. В 11А их четыре —
класс нерешаем арифметически. Из-за одного класса из 28 завуч был обязан
ослабить норму целиком, и её начинали нарушать все остальные, кому она была
вполне по силам. Теперь послабление точечное: норма ослабляется только в том
классе, который её физически выполнить не может, а система сама называет
класс, причину и выход. Результат на тех же данных:

| | было | стало |
|---|---|---|
| Нарушений санитарных норм | 32 | **2** (оба в 11А, объяснены заранее) |
| Окон у учителей | 177 | **54** |

**2. По умолчанию давалось 30 секунд на поиск.** Замерено: со всеми нормами
в жёстком режиме первое законное расписание на 28 классах находится за 119 с.
То есть завуч получал бы «решения нет» там, где решение существует. Значение
по умолчанию — 5 минут, минимальное — минута, с подписью «только для маленькой
школы».

**3. «Не успел» показывалось как «решения нет».** UNKNOWN (не хватило времени)
и INFEASIBLE (доказано, что расписания нет) — разные ответы, требующие разных
действий. Раньше оба выводились одинаково, и на коротком бюджете завуч читал
разбор про санитарные нормы, хотя виновато было время. Теперь UNKNOWN даёт
кнопку «искать дольше», INFEASIBLE — разбор причин.

**4. `diagnose()` мог оговорить невиновную норму.** Он перебирает нормы по одной
с бюджетом в секунды и раньше засчитывал в вину любой неуспех — включая
«не успел». На большой школе это гарантированно указывало бы на случайную
норму. Теперь виновной считается только доказанная невозможность.

**5. Введено автосохранение.** Данные пишутся на диск при каждом изменении,
а не по кнопке. Ввод школы — это часы работы, и терять их на закрытой вкладке
недопустимо. Проверено: 500 строк сериализуются за 1,6 мс, на отзывчивость
не влияет.

Проверено также: приложение выполняется без исключений (headless-прогон
штатным раннером Streamlit), Excel и HTML собираются, кабинеты назначаются
всем 980 урокам, конфликтов в сетке ноль, окон у классов ноль.

**Итог на дефолтных настройках (5 минут):** 980 уроков, 2 нарушения норм,
0 конфликтов. Система готова к тому, чтобы её показать завучу.

⚠️ Не изменилось: **реальных данных школы по-прежнему нет.** Всё выше —
модельная школа, построенная по типовому плану № 75. Колонки «было»
для сравнения не существует, пока нет расписания реальной школы.

## Сделано

- [x] Ресёрч рынка и конкурентов (см. `CLAUDE.md` §3)
- [x] Выяснена ситуация в Беларуси: централизованного автосоставления нет,
      schools.by только отображает расписание (§4.1)
- [x] Найдено ключевое событие: «Электронная школа» ГИАЦ, пилот 300+ школ
      с 01.09.2026, полный охват к 2027 (§4.2)
- [x] Определена стратегия: не конкурировать с ГИАЦ, а быть модулем внутри (§4.3)
- [x] Посчитана экономика — как источник дохода не рассматривать (§4.4)
- [x] Определена точка выхода до старта (§10)
- [x] Создан скелет проекта
- [x] Выбрано название: **ЛАД** — Логистика Академического Дня
      (21.08.2026). Занятость имени в edtech РБ не проверялась.
- [x] Решён формат системы: CLI → один самодостаточный HTML-файл,
      пайплайн `солвер → schedule.json → рендер` (§7.1). Эволюция форматов — §5.1
- [x] **Смена стратегии валидации (21.08.2026):** запросы в ГИАЦ/Минобр отложены.
      Первый зритель демо — завуч школы Влада, личный контакт (§8.1, §10 фильтр 0)
- [x] Изучена предметная область школы РБ → `docs/domain.md`: 40+ вводных
      со статусами достоверности. Согласованы 4 развилки (§10 domain.md):
      охват V–XI, смены в схеме, нормы внешним конфигом, предметы из данных школы
- [x] Схема данных `src/lad/model.py` — держит группы/деления, межклассные
      профильные группы, две смены, шестой день, факультативы как занятость
- [x] Солвер `src/lad/solve.py`: HARD 1, 2, 4, 6. Hello-world на 3 классах
      решается за 0.04 с, OPTIMAL, деление на подгруппы работает
- [x] **Найдено новое ограничение HARD-9** (синхронность подгрупп) —
      см. `docs/domain.md` §4.7
- [x] Реализованы HARD-8 (нет окон у класса) и HARD-9. Проверено на синтетике:
      окон у классов 0, разошедшихся делений 0
- [x] **Стресс-тест 24 класса / 768 часов: OPTIMAL за 0.43 с.** Масштаб реальной
      школы солвер держит — главный технический риск снят
- [x] Найдено: `INFEASIBLE` без объяснения — продуктовая проблема (`domain.md` §4.8)
- [x] **HARD-3 и HARD-5 (кабинеты)** через ёмкость типа кабинета + назначение
      конкретных номеров после решения (`assign_rooms`)
- [x] **`validate.py`** — один валидатор для «было» и «стало» (§8.3)
- [x] **`storage.py`** — `schedule.json`, граница солвер↔рендер, round-trip проверен
- [x] **`render.py`** — самодостаточный HTML, 18 КБ, ноль внешних запросов,
      светлая/тёмная тема, переключатель «было/стало», ховер = учитель + кабинет
- [x] **`app.py` + `tables.py`** — рабочее место на Streamlit. Сборка School
      из таблиц вынесена из UI, поэтому тестируется без запуска Streamlit
- [x] **Санитарные нормы РБ добыты и оцифрованы** (`domain.md` §12): постановление
      № 206 через web.archive.org, ранговая шкала трудности V–XI по годам обучения
- [x] Реализован **п. 88.2** — равномерность нагрузки по дням с учётом трудности.
      На примере: разброс по трудности 72 → 33
- [x] Диагностика ошибок ввода до солвера: «у учителя X 60 часов, а в сетке 35»
- [x] **День 4: SOFT 1, 2, 3, 5 + целевая функция.** На 24 классах за 120 с:
      окон у учителей **461 → 0**, выходов учителей 337 → 260, разброс 125 → 57
- [x] Замерена кривая сходимости (10/30/60/120 с) — `docs/domain.md` §11
- [x] Найдена и исправлена ошибка формулировки окон: наивное «занят слева
      и справа → занят здесь» пропускает дыру в два урока подряд. Вскрылось
      тем, что метрика считается независимо от солвера (§8.3 CLAUDE.md)
- [x] Окружение поднято: `.venv` на Python 3.13, ortools 9.15.6755, pandas 3.0.5.
      Тестовая CP-SAT-модель решается (OPTIMAL). Под Python 3.14 колёс ortools нет

- [x] **Экспорт в Excel** (`src/lad/excel.py`, openpyxl): четыре листа —
      по классам (сетка как в коридоре), по учителям (личное расписание каждого),
      по кабинетам (кто где и когда), проверка (метрики + список нарушений).
      Есть обезличенная версия для показа третьим лицам (§8.4)
- [x] **Ввод перестроен под последовательность.** Вкладки пронумерованы в порядке
      зависимостей: классы → предметы → учителя → кабинеты → нагрузка → пожелания.
      Наверху строка состояния «✅ Классы — 8 · ⬜️ Предметы — 0», подсказка
      «Дальше: Предметы» и объяснение, зачем шаг нужен и что будет, если пропустить.
      Вкладка, для которой не хватает данных, говорит об этом прямо
- [x] **В нагрузке выпадающие списки** вместо ручного набора: класс, предмет
      и учитель выбираются из заведённых — опечатка стала невозможной
- [x] Исправлено: пустые данные показывались как заполненные (подставлялся бланк
      со строкой-примером «Иванова И.И.», и она считалась данными школы)
- [x] Исправлено: таблица сверки роняла интерфейс — колонка со смесью чисел
      и «—» не сериализуется (pyarrow не берёт смешанный тип)

- [x] **Левое меню переписано на язык завуча.** Было шесть ползунков «весов
      штрафов» — язык солвера, непонятно, надо ли их трогать. Стало: «Чьё удобство
      важнее» с тремя вариантами (Поровну / Учителям удобнее / Детям легче),
      у каждого написано, чем платишь; «Сколько ждать» вместо «бюджет времени,
      секунд»; ползунки убраны в свёрнутую «Тонкую настройку». Все поля «Школы»
      получили подсказки
- [x] Исправлено: при смене приоритета ползунки держали старые значения —
      Streamlit кеширует виджет по ключу. Ключи привязаны к выбранному варианту

- [x] **Оформление приведено к одному стилю** (24.08.2026, референс — монохромный
      дашборд): тема в `.streamlit/config.toml` — чёрный/белый/серый, цвет остался
      только за смыслом (нарушение красное, сошлось зелёное); заголовки антиквой
      (Source Serif 4), текст и таблицы — Inter; чёрный сайдбар, рабочее место
      белым листом на сером поле; убраны кнопка Deploy и все эмодзи из интерфейса
      (Material Symbols вместо них). Правки, которых темой не выразить, — в
      `src/lad/style.py`, один CSS-блок с объяснением каждого правила
- [x] **Сводный вид «вся школа»** в HTML (`render.py`): переключатель
      «Один класс / Вся школа». В сводном режиме строки — день × урок, столбцы —
      все классы, то есть тот самый лист, который висит в учительской. Шапка
      и колонка дня залипающие, предметы сокращены как от руки (Бел. яз., Физ-ра,
      ОБЖ), кабинет мелким шрифтом, полное название и учитель — по наведению.
      Есть фильтр по дню. Пустые последние уроки дня не рисуются.
      В Excel такой лист был и раньше («Классы»)
- [x] **Нагрузочная проверка на условной городской школе** (`src/lad/demo_city.py`,
      запуск `PYTHONPATH=src .venv/bin/python -m lad.demo_city`): 28 классов V–XI
      по четыре в параллели, 60 учителей, 36 кабинетов, 500 строк нагрузки,
      980 часов, восемь профильных классов X–XI. Часы — из типового плана № 75
      через тот же `generate_load`, которым пользуется завуч; ФИО и кабинеты
      придуманы. Кабинетный фонд не задаётся «на глаз», а считается из нагрузки
- [x] **Ввод нагрузки перевёрнут: режим «Назначить учителей»** (24.08.2026,
      по требованию Влада — построчный ввод 500 строк невозможен). Класс,
      предмет и часы система уже знает из типового плана; вручную нужно только
      «кто ведёт». Поэтому вместо таблицы: выбираем предмет → каждому учителю
      отмечаем его классы тегами. Математика на 28 классов — это 7 учителей
      вместо 28 строк. Есть:
  - полоса «назначено N мест из M» и список предметов, где незакрытые сверху
  - **автораздача «Разложить поровну»** — черновик по часам между выбранными
    учителями; подгруппы одного класса намеренно достаются РАЗНЫМ людям
    (они занимаются одновременно)
  - часы каждого учителя рядом с его именем и отметка «больше 30 ч — выше
    полутора ставок, проверьте»
  - снятая галочка освобождает место, поэтому переиграть можно не открывая
    большую таблицу
  - полная таблица никуда не делась — она вторым режимом, для тонких правок
- [x] **Настройка солвера: `linearization_level` по фазам** (25.08.2026,
      улучшение №1 из списка идей). Замерено пять конфигураций по три минуты
      на 28 классах:
  | настройка | штраф | окон у учителей |
  |---|---|---|
  | как было | 7467 | 236 |
  | **linearization_level=2** | **6947** | **143** |
  | symmetry_level=4 | 7321 | 228 |
  | probing_level=2 | 7985 | 246 |
  | interleave+LNS | — | решения не нашёл вовсе |

      Контрольный прогон на полном бюджете (10 минут): окон у учителей 51 → 37,
      выходов в школу 287 → 276. Симметрия не сработала ожидаемо — классы
      в параллели различаются учителями, переставлять их нечего.
      ⚠️ Уровень разведён по фазам: поиску ПЕРВОГО расписания релаксация мешает
      (48 секунд вместо четырёх), оптимизации помогает
- [x] **Найден баг: полная оптимизация не выполнялась вовсе.** Трассировка
      вызовов солвера показала `MODEL_INVALID` за ноль секунд на последнем
      этапе. Причина — повторный `AddHint` для той же переменной: подсказка
      добавляется второй записью и модель становится невалидной. Лечится
      `ClearHints()` перед новой подсказкой. До исправления наружу уходил
      результат промежуточного этапа
- [x] **Иерархическая оптимизация: проверена и ОТКЛОНЕНА** (улучшение №2).
      Идея — сперва выжать окна учителей, потом остальное. Первый вариант
      (жёсткая фиксация достигнутого) заперал поиск: этап выходил на 145-й
      секунде из 300, половина бюджета пропадала. Мягкий вариант (первый этап
      как разгон, дальше свободный поиск) выигрыша тоже не дал: по два прогона
      каждого режима — окон 92 и 86 у обычной суммы против 89 и 105 у этапов.
      Разброс между прогонами больше разницы между режимами. Выключено
      по умолчанию, код и замеры оставлены в комментарии
- [x] **Закрепление уроков: «пересобери только эти классы»** (25.08.2026,
      выбор Влада из списка идей). `solve(..., pinned=[...])` требует, чтобы
      указанные уроки остались в своих слотах; на экране результата — выбор
      классов для пересборки и кнопка «Пересобрать выбранное»: остальное
      закрепляется само. Это ответ на первый вопрос завуча к готовому
      расписанию — «вот здесь надо подвинуть»: пересобирать всё нельзя,
      вместе с неудобным уроком переедет и то, что устраивало.
      Проверено: 62 из 62 закреплённых уроков остались на местах
- [x] **⚠️ Найдена и устранена нестабильность солвера (25.08.2026).**
      Жёсткий коридор «ровно 5–6 уроков в дне» пережал модель: на 28 классах
      поиск ЛЮБОГО расписания стал случайным — то 10 секунд, то ничего
      за 45 (три прогона из трёх разошлись, система отвечала «решения нет»
      при том, что решение есть). Коридор получил допуск в один урок
      в каждую сторону, а к идеалу тянет штраф. После правки — 4–5 секунд
      стабильно, три прогона из трёх. Заодно: первой фазе отдан весь бюджет
      (фиксированные 10 с были ещё одной причиной случайных отказов),
      число потоков — по числу ядер вместо восьми
- [x] **Экран результата больше не пропадает после скачивания** (25.08.2026).
      Скачивание файла в Streamlit — это перезапуск скрипта, а показ результата
      был одноразовым (флаг `job_shown`): нажал «Excel» — и вся страница
      с метриками, нарушениями и предпросмотром исчезала. Теперь готовое
      расписание собирается ОДИН раз и лежит в состоянии сессии (отчёт, HTML,
      Excel), а экран держится, пока не нажмёшь «Составить заново». Проверено
      сценарием в браузере: после скачивания `raspisanie.xlsx` результат на месте
- [x] **Обезличенные версии убраны из интерфейса** (решение Влада 25.08.2026).
      Были две лишние кнопки — «Excel обезличенный» и «HTML обезличенный».
      Код анонимизации в `render.py` и `excel.py` остался (параметр `anonymize`),
      вернуть в интерфейс — три строки. ⚠️ Учесть при контенте: `CLAUDE.md` §8.4
      требует обезличивания для блога, видео и любого показа третьим лицам,
      теперь это делается только из кода
- [x] **Метрики результата разложены по четыре в ряд** — восемь в одну строку
      сжимались так, что подписи обрезались многоточием
- [x] **Один список вместо двух: в классах предмета видны ВСЕ классы школы.**
      Влад: «не могу понять, почему не выбираю из списка всех наших классов».
      Разделение «раздать существующее» / «добавить класс отдельным блоком»
      было искусственным: завуч ищет класс там, где выбирает учителя, и не
      обязан знать, есть ли строка в таблице нагрузки. Теперь в списке все 28
      классов: занятые подписаны хозяином, отсутствующие помечены «+ завести»
      и создаются выбором. Часы берутся из типового плана; если план про эту
      параллель молчит (астрономия в V классе) — система говорит об этом
      и просит число, а не придумывает
- [x] **Два бага, вскрытые той же проверкой:**
  - **бесконечная перерисовка**: выбор класса, который не удалось завести,
    запускал перезапуск снова и снова — страница мигала, а причина не успевала
    показаться. Теперь перерисовка только если данные правда изменились
  - **назначение свежезаведённого класса молча терялось**: Streamlit, увидев
    ключ виджета в session_state, ИГНОРИРУЕТ новое значение `default`, и список
    продолжал показывать прежний выбор. Ключи виджетов теперь включают
    отпечаток текущей раскладки предмета: меняются данные — виджет строится
    заново. Проверено обоими сценариями: класс заводится и сразу получает
    учителя; передача класса между учителями не сломалась (500 из 500)
- [x] **Добавление предмета классам, где его нет** (`add_subject_slots`).
      Влад попробовал дать астрономию восьмому классу и упёрся в «No results»:
      режим назначения раздавал существующие места, но не создавал новые,
      а типовой план даёт астрономию только в XI. Теперь в том же экране есть
      блок «Добавить предмет классам, где его ещё нет»: часы подставляются
      из плана для этой параллели, а если план про неё молчит — система прямо
      говорит об этом и просит число часов. Делимый предмет заводится сразу
      двумя строками (подгруппы «1» и «2»), дубли не создаются.
      Попутно: блок остаётся раскрытым, пока в нём что-то выбрано — иначе
      после выбора класса перерисовка схлопывала его вместе с кнопкой
- [x] **Два бага режима назначения, найдены Владом сразу (24.08.2026):**
  - **нельзя было добавить второго учителя**: в списке классов показывались
    только СВОБОДНЫЕ места, а когда первый учитель разобрал предмет целиком,
    второму нечего было выбрать («No results»). Теперь видны все классы
    предмета, занятые подписаны хозяином («9А · Кузьмич Е. П.») — выбрать
    такой значит забрать себе, у прежнего он пропадает
  - **назначения слетали пачками**: ключи виджетов были привязаны к номеру
    строки, а порядок учителей меняется после каждой передачи класса —
    значения доставались соседям. На проверке потерялось 11 классов из 12.
    Ключи привязаны к ИМЕНИ учителя, список отсортирован по алфавиту,
    состояние пустой строки очищается после применения. Проверено сценарием
    в браузере: после передачи 9А назначено 500 из 500, ничего не потеряно
- [x] **Живой прогресс составления** (`src/lad/job.py`, `Progress`/`_Reporter`
      в `solve.py`, фрагмент `show_progress` в `app.py`). Солвер уехал в поток —
      раньше страница замирала на все десять минут, ни прогресса, ни возможности
      прервать. Теперь на экране: этап («ищу законное расписание» → «законное
      уже есть, улучшаю»), потраченное время из бюджета, **живые человеческие
      метрики** (окна у учителей, разброс дней, нарушения норм — читаются прямо
      из переменных модели на каждом улучшении, со стрелкой «было → стало»),
      «расписание стало лучше на N% с первой версии», график падения штрафа
      и кнопка «Остановить и взять текущее». Проверено на 28 классах: за 44 с
      окна у учителей 1420 → 180, найдено 125 улучшений.

      Честная оговорка вынесена в интерфейс: обычного «осталось три минуты»
      быть не может — солвер либо доказывает оптимальность и останавливается
      сам, либо работает до конца бюджета. Теоретический запас улучшения (gap)
      показывается отдельной подписью с пометкой, что это верхняя оценка:
      на большой школе он остаётся 90+%, даже когда расписание уже хорошее
- [x] **Исправлено: при коротком бюджете система отвечала «решения нет».**
      Вскрылось при проверке прогресса: 28 классов и 30 секунд → UNKNOWN и ноль
      уроков, хотя законное расписание находилось за две секунды. Причина —
      попытка пересчитать его заново после снятия целевой функции. Теперь
      решение первой фазы забирается сразу и отдаётся, если улучшение
      не успело. Проверено: 30 с → FEASIBLE, 980 уроков
- [x] **Коридор уроков в дне (24.08.2026, по требованию Влада).** Одного штрафа
      за разброс не хватало: на большой школе солвер экономил его последним
      и оставлял класс с днём из двух уроков рядом с днём из восьми. Теперь
      число уроков в дне зажато в коридор из недельной нагрузки самого класса
      — `[нагрузка // дней, ceil(нагрузка / дней)]`, то есть 5–6 уроков при 28
      часах и 7 при 35. Коридор арифметически всегда достижим, поэтому сам
      по себе задачу не ломает; вынесен на вкладку «Нормы» переключателем
      «Ровное число уроков в дне» (по умолчанию жёстко, в «мягко» отклонение
      штрафуется втрое). Считается по СЛОТАМ: две подгруппы иностранного стоят
      классу одного урока, а не двух. В отчёт добавлена метрика «Самый короткий
      день» — средний разброс её прятал.

      Проверено на 28 классах, бюджет 600 с:
  | метрика | без коридора | с коридором |
  |---|---|---|
  | Разброс нагрузки по дням | 126 | **24** |
  | Разброс по трудности (п. 88.2) | 1127 | **431** |
  | Самый короткий день по школе | — | **5 уроков** |
  | Окон у учителей | 49 | 64 |
  | Нарушений норм | 29 | 28 |
  | Окон у классов, конфликтов | 0 | 0 |

      Плата — 15 лишних окон у учителей: ровные дни у детей и плотное
      расписание у учителей тянут в разные стороны, это ровно тот компромисс,
      который выбирается переключателем «Чьё удобство важнее»
- [x] **Найдена и устранена главная проблема масштаба: солвер стал двухфазным.**
      Замеры на 28 классах (980 часов) показали, что выполнимость не проблема,
      а оптимизация — да:
  | что искали | статус | время | окон у учителей |
  |---|---|---|---|
  | любое законное расписание, нормы п. 94 жёстко | OPTIMAL | 13 с | 456 |
  | любое законное, нормы п. 94 мягко | OPTIMAL | 2 с | 409 |
  | с оптимизацией (старый код) | **UNKNOWN** | 600 с | — |
  | с оптимизацией (двухфазный) | FEASIBLE | 600 с | **49** |

      С целевой функцией CP-SAT за 10 минут не находил НИ ОДНОГО решения —
      завучу возвращать было нечего. Теперь `solve()` сперва ищет любое законное
      расписание (четверть бюджета), отдаёт его подсказкой `AddHint` и только
      потом улучшает; если улучшить не успел — возвращает найденное.
      На выходе всегда есть расписание. Окон у классов и конфликтов в сетке — 0
      во всех прогонах; нарушений норм в итоговом — 29 (нормы п. 94 стоят «мягко»)
- [x] **Найдено противоречие в нормах (24.08.2026).** Первый прогон вернул
      INFEASIBLE, диагностика указала на «физкультуру не два дня подряд».
      Разбор: класс спортивно-педагогического профиля получает по типовому плану
      **4 часа физкультуры**, а п. 94 ССЭТ при пятидневке вмещает максимум 3
      (Пн, Ср, Пт). Сдвоенным уроком не закрыть — п. 65 ССЭТ прямо запрещает
      объединять физкультуру. Один класс из 28 обрушивал весь прогон.
      Добавлена проверка в `check_norms`: предупреждение выдаётся **до** запуска
      солвера, с указанием класса и вариантов (ослабить норму или перенести час
      в шестой школьный день). **Стоит спросить у завуча, как школа решает это
      на практике** — вероятно, норма нарушается молча
- [x] **Второе следствие того же прогона:** фонд спортзалов нельзя считать
      по среднему за неделю. Норма «через день» ставит физкультуру всех классов
      в одни и те же три дня, поэтому зал нужен по пиковому дню. В генераторе
      это учтено (`_rooms_for`)
- [x] **Разбор интерфейса по экранам** (24.08.2026, обход скриншотами всех вкладок,
      шести шагов мастера и экрана результата). Найдено и исправлено:
  - главная кнопка в тёмной теме выглядела отключённой: Streamlit пишет на
    primary-кнопке ВСЕГДА белым, значит в монохроме пришлось бы брать мутно-серый
    фон. Кнопка перекрашена вручную — плашка цвета текста, чёрная на светлой теме
    и светлая на тёмной
  - **баг ввода: сетка пожеланий и колонки «кабинет» были забиты словом «None»** —
    выпадающий список рисует так пустую строку среди вариантов. Введено видимое
    «—», старые сохранённые данные мигрируются при загрузке
  - мастер вынесен на карточку шириной 720 px: форма во всю ширину экрана
    нечитаема. Кнопки «Назад»/«Далее» прижаты к правому краю и меряются
    по содержимому — раньше стояли в колонках [1,1,2] и разъезжались
  - `st.info` синей плашкой выбивался из монохрома — подложки уведомлений
    приглушены до фона документа, цвет остался только у ошибки и успеха
  - переключатели строгости норм: `st.radio(horizontal)` → `st.segmented_control`,
    названия норм с заглавной буквы, пункт документа отдельной подписью
  - ширина полосы набора ограничена 1200 px — строка в 1500 px нечитаема
  - предпросмотр расписания переведён с устаревшего `st.components.v1.html`
    на `st.iframe`
- [x] **Проверка вёрстки своими глазами**: playwright + headless chromium в `.venv`
      (только для разработки, в `requirements.txt` не добавлен). Скриншоты снимаются
      из терминала — не нужно просить Влада присылать их вручную
- [x] **Интро при первом запуске** (`src/lad/intro.py`) — шесть карточек-слайдов
      «как это работает»: что делает программа, что нужно ввести, почему вкладки
      пронумерованы, чем «не может» отличается от «нежелательно», что нормы уже
      внутри, что на выходе. Кнопок ровно две — «Далее» и «Пропустить».
      Показывается один раз (`settings.intro_seen`), пересматривается кнопкой
      «❔ Как это работает» в сайдбаре. Проверено через `streamlit.testing.AppTest`

## Как запустить

```
cd "$HOME/Documents/vs code proj/MinObr" && .venv/bin/streamlit run app.py
```

⚠️ Тильда внутри кавычек в zsh НЕ раскрывается — `cd "~/Documents/..."` даст
`no such file or directory`. Использовать `$HOME` внутри кавычек либо `~/` снаружи.

Откроется рабочее место: вкладки Классы / Предметы / Учителя / Кабинеты /
Нагрузка и кнопка «Составить». Данные лежат в `data/school.json`
(сейчас — пример на 3 класса), результат — `out/raspisanie.html`.

## Следующие шаги

- [ ] **Влад: достать реальное расписание своей школы** (schools.by / сайт школы /
      фото), положить в `data/raw/`. См. `CLAUDE.md` §8.1. **Единственный блокер.**
      Всё остальное готово; без него нет колонки «было» и трёх цифр демо (§6)
- [ ] Типовые учебные планы Минобразования — там предельная нагрузка и
      продолжительность урока, которых нет в СанПиН (`domain.md` §12)
- [ ] День 2: парсер реального расписания школы → нагрузка
- [ ] Нормы про контрольные (п. 67, ИМП 9.6) оцифрованы, но в солвере не заданы:
      в данных нет отметки «здесь контрольная». Нужен признак в нагрузке —
      или честно сказать завучу, что это вне модели MVP
- [x] ~~Сдвоенные уроки не реализованы~~ — **реализованы 24.08.2026.** Вскрылось
      на данных школы: класс X с физмат-профилем не составляется в принципе.
      Математика на повышенном уровне — 6 часов, а пятидневка при правиле
      «предмет не дважды в день» вмещает максимум 5. Ровно для этого в п. 65
      ССЭТ и есть разрешение на сдвоенные уроки в VIII–XI на повышенном уровне
      и по трудовому обучению. Реализовано с требованием идти ПОДРЯД
- [x] **Профильные классы X–XI.** Уровень изучения (базовый/повышенный) — колонка
      в нагрузке. Класс, где хоть один предмет на повышенном уровне, автоматически
      считается профильным: это меняет предельную недельную нагрузку. Часы
      повышенного уровня — верхняя граница диапазона типового плана
      («4–6» у математики = 4 базовый, 6 повышенный). Готовые наборы профилей
      (физмат, химбио, филологический, обществоведческий) — в `data/plan_75.json`
- [x] **Сверка с учебным планом** — вкладка «Учебный план»: что положено классу
      по типовому плану, что введено, и все расхождения. Ловит забытый предмет
      («НЕ ВВЕДЁН»), лишние часы, посторонние предметы, нехватку часов
      в профильном классе. Без неё забытая физика обнаружилась бы в сентябре
- [x] **Тип занятия** (урок / факультатив / стимулирующее / классный час) —
      колонка в нагрузке. Факультативы занимают учителя и кабинет, но входят
      в предельную нагрузку иначе (п. 93 ССЭТ)
- [x] **Подгруппы могут идти в разные кабинеты** — труд у мальчиков в мастерской,
      у девочек в кабинете обслуживающего труда. Раньше тип кабинета задавался
      предметом целиком, и обе подгруппы просились в одну мастерскую
- [x] **Проверка кабинетов для подгрупп до солвера**: «2 подгруппы должны
      заниматься одновременно, а компьютерных кабинетов всего 1»
- [x] **Данные модельной школы**: 8 классов V–XI (5А, 5Б, 6А, 7А, 8А, 9А,
      10А физмат, 11А химбио), 17 учителей, 23 предмета, 21 кабинет, 269 часов.
      Часы — из типового плана № 75, сверены суммами по каждой параллели.
      ФИО и кабинеты — модель: **реального расписания школы всё ещё нет**
- [ ] Ввод «было» — текущего расписания школы. **Вопрос к Владу открыт:**
      вводим его в систему (сравнение честное) или берём время со слов завуча
- [x] ~~SOFT-4, 6, 8 — в СанПиН таких требований НЕТ~~
      ❌ **Это было неверно (исправлено 23.08.2026).** Требования есть, но не
      в СанПиН № 206, а в ССЭТ (постановление Совмина № 525) — искали не в том
      документе, хотя файл лежал в `data/raw/`. Найдено и реализовано:
  - **п. 94 ССЭТ** — пик нагрузки во вторник, среду **и (или) пятницу** для V–XI
    (не «вторник–среда», как считалось); вторник/среда для I–IV
  - **п. 94 ССЭТ** — физкультура не два дня подряд в одном классе; не чаще
    одного раза в неделю первым или последним уроком
  - **п. 94 ССЭТ** — трудные предметы (математика, рус/бел/иностранный язык,
    физика, химия) на первом или последнем уроке не чаще раза в неделю
  - **п. 65 ССЭТ** — сдвоенные уроки: где допускаются (труд кроме I–IV,
    повышенный уровень VIII–IX и X–XI, контрольные и лабораторные;
    физкультуру объединять нельзя). Раньше стояло «правил не найдено»
  - **п. 67 ССЭТ + ИМП 2025 п. 9.6** — контрольные: не более одного предмета
    в день в классе, не на последнем часе, не в пятницу (I–IV), не после
    третьего часа в пятницу (V–XI)
  - **п. 92 ССЭТ** — вторая смена запрещена в I, II, V, IX–XI и в VIII
    с повышенным уровнем
  - **п. 93 ССЭТ** — «Час здоровья и спорта» не входит в предельную нагрузку;
    творческие факультативы дают +3 часа в V–XI
- [x] **Предельная нагрузка уточнена**: теперь два столбца — базовый уровень
      и повышенный (V: 28/29, VI: 31/32, VII: 31/32, VIII: 32/33), плюс I–IV.
      ⚠️ Зафиксировано расхождение первоисточников: приложение 16 ССЭТ и типовой
      план № 75 дают разные цифры (IX: 33 против 34, X–XI: 34 против 35).
      Взят типовой план — п. 93 ССЭТ сам на него ссылается, и он новее
      (23.04.2025 против редакции 2024). Расхождение прописано в конфиге
- [x] **Единственное, чего нет ни в одном документе** — прямой лимит уроков
      в день. Проверены СанПиН, ССЭТ с приложениями 12 и 16, типовые планы, ИМП.
      Так и записано, цифра не выдумана
- [x] **Нормы п. 94 реализованы в солвере** с переключателем строгости
      (жёстко / мягко / не применять) — норма это требование к школе, а не
      к алгоритму, и завуч решает, какую ослабить
- [x] **`diagnose()` — объяснимость вместо голого INFEASIBLE.** Снимает нормы,
      проверяет, что дело в них, включает по одной и называет виновную человеческим
      языком. Проверено: точно указала «трудный предмет на краю дня»
- [x] **Валидатор считает нарушения норм п. 94** и делит их со структурными
      конфликтами: «учитель в двух классах» — расписание нельзя провести,
      «физкультура два дня подряд» — можно, но это нарушение постановления
- [x] **Синтетика переписана по первоисточнику** — нагрузка V класса из типового
      плана № 75, 27 часов, сумма совпала с указанным в документе объёмом.
      Старая выдуманная синтетика давала INFEASIBLE: в ней не было «лёгких»
      предметов, чтобы закрыть первые и последние уроки дня
- [x] **Ввод переделан под ручное заполнение** (решение Влада 23.08.2026):
  - мастер первого запуска из 6 шагов; дальше завуч живёт в таблицах
  - нагрузка собирается черновиком по типовому плану № 75 — остаётся вписать,
    кто ведёт. Там, где в документе дробь по полугодиям, значение пустое
    и помечено «уточните», а не додумано
  - пожелания учителей — сетка день × урок, отдельно «✗ не может» (запрет)
    и «~ нежелательно» (штраф). Проверено: запрет соблюдается, пожелание тоже
  - вкладка «Нормы» с переключателями строгости и ссылками на первоисточник
  - `data/plan_75.json` — справочник учебного плана V–XI
- [x] Починен краш при пустом `data/school.json` (KeyError 'load')
- [x] Проверено: демо-сценарий §6 работает. На синтетике по типовому плану
      **нарушений санитарных норм 10 → 0**, окон у классов и учителей 0

## Открытые вопросы

| # | Вопрос | Статус | Почему важно |
|---|---|---|---|
| 1 | Пишет ли ГИАЦ модуль расписания сам? | ⏸ отложено до MVP | определяет судьбу проекта (§10), но запрос не шлём до вердикта завуча |
| 2 | Санитарные нормы РБ | ✅ **добыты 22.08.2026** через web.archive.org, оцифрованы в `data/sanpin_by.json` (`domain.md` §12) |
| 2а | Предельная недельная нагрузка и продолжительность урока | ✅ **закрыт 23.08.2026.** Нагрузка — типовой план № 75 (базовый и повышенный уровень, I–XI), урок 45 мин — ССЭТ № 525 прил. 12 |
| 2б | Максимум уроков в день | ❗ **прямой нормы нет ни в одном документе** — проверены СанПиН, ССЭТ с приложениями, типовые планы, ИМП. Выводится косвенно из недельной нагрузки и времени окончания смены |
| 2в | Расхождение первоисточников по предельной нагрузке | ⚠️ прил. 16 ССЭТ vs типовой план № 75 (IX: 33/34, X–XI: 34/35). Взят типовой план, расхождение зафиксировано в конфиге. **Стоит спросить у завуча, какой цифрой пользуется школа** |
| 3 | Есть ли доступ к реальной нагрузке школы? | 🔜 путь есть | публичное расписание школы Влада → нагрузка выводится из него, оно же наше «было» (§8.1) |
| 4 | Входит ли расписание в дорожную карту «Электронной школы» на 2 этап? | ❓ по открытым источникам не видно | сайты edu.gov.by / giac.by не открываются с зарубежных IP |
| 5 | Видны ли в публичном расписании учителя и подгруппы? | ❓ | без учителей модель строится иначе; подгруппы — главная ловушка (§8.5) |
| 6 | Вердикт завуча: можно ли ставить в работу? | ❓ день 7 | фильтр 0 точки выхода (§10) |

## Заметки

- Сайты `.by` (edu.gov.by, giac.by, schools.by, adu.by) отдавали ECONNREFUSED
  при попытке фетча 21.08.2026. Открывать вручную или искать зеркала.
- Цифры по нормам НЕ выдумывать. Не подтверждено → `UNVERIFIED`.
