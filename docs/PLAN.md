# План: продукт ЛАД за 5 дней (14.09 → 19.09.2026)

## Почему план поменялся

13–14.09 через амбассадора Startup Space о проекте рассказали министру:
«ребята поддержат, делай продукт». Это та ветка точки выхода §10 CLAUDE.md
(«есть интерес → строим продукт из §5»), и запреты §6 («без UI, без БД,
без деплоя») больше не действуют. Действует новый таймбокс: **пять дней**.

Что нужно показать через пять дней — не демо, а продукт, который завуч может
открыть и сам, без нас, пройти путь:

> загрузил нагрузку → получил расписание → поправил руками, и система на ходу
> объясняет, почему сюда нельзя → забрал в Excel или распечатал → в понедельник
> учитель заболел, и система предложила замены.

## Что уже есть и остаётся

Ядро на Python — `src/lad/`: солвер CP-SAT, санитарные нормы РБ по
первоисточникам, валидатор, разбор причин, если расписание не складывается,
выгрузка в Excel и HTML. **Ядро не переписываем**, продукт строится поверх него.

Streamlit (`app.py`) живёт до 19.09 как запасной вариант для показа и затем
выключается.

## Архитектура

```
web/     React + Vite + TypeScript + Tailwind — интерфейс
server/  FastAPI — HTTP-обёртка над src/lad, SQLite, фоновые задачи солвера
src/lad/ ядро (без изменений в интерфейсе функций)
```

- **Школа** хранится одним JSON-документом в SQLite (тот же формат, что сейчас
  `data/school.json`), расписания — версиями. Миграции схемы не нужны.
- **Солвер** работает в отдельном процессе: прогресс идёт в браузер через SSE,
  «стоп» гарантированно останавливает поиск, и падение солвера не роняет сервер.
- **Правка вручную** проверяется на сервере тем же `lad.validate`, что считает
  итоговые цифры, поэтому то, что показывает сетка, совпадает с отчётом.
- **Деплой**: Railway, один сервис (FastAPI отдаёт собранный фронт).
  Нужно минимум 4 vCPU — меньше солвер не тянет (`available_cpus` в solve.py).

## Дни

### День 0 — понедельник 14.09 (сегодня)
- [x] Вернуть учителей в стартовую школу (приложение не могло составить расписание)
- [x] Защита автосохранения от тихого опустошения таблиц
- [x] Анонимизация на экране результата — для Startup Day
- [x] Пуш в `main` → Streamlit к Startup Day
- [x] **Надёжность солвера: ответ «не успел» больше не приходит никогда.** (закрыто ниже)
      Сделано: черновик с нормами-штрафами и целью «ноль нарушений» (сетка
      за ~1 с вместо 86–300+ с), затем подсказкой в модель с нормами-запретами.
      Идёт замер. Критерий: 5 прогонов из 5 дают расписание за ≤ 5 минут.
- [x] Каркас `server/`: школы с ревизиями, солвер в отдельном процессе, SSE
- [x] Каркас `web/`: школы, составление с живым ходом, сетка расписания
- [x] Сделано раньше плана (из дня 2): `lad/explain.py` и ручки `/heatmap`, `/move` —
      ход как обмен уроков, подсветка всех клеток, причины человеческим языком
- [ ] Деплой на Railway — ждёт `railway login` от Влада (CLI не авторизован)
- [x] Дизайн-основа: `docs/DESIGN.md`, токены, кирпичи `web/src/ui/`, шапка сверху
- [x] Сделано раньше плана (из дня 3): раздел «Данные» — школа, классы по параллелям,
      кабинеты с проверкой «хватает ли», предметы и черновик нагрузки из плана № 75,
      назначение учителей по предмету, раздача поровну, учителя вставкой из Excel,
      пожелания кликом по неделе. Автосохранение ревизиями. Импорт чужой тарификации — ещё нет.
- [x] Сделано раньше плана (из дня 4): замены — кандидаты с причинами на каждый урок
      отсутствующего учителя и лист замен на печать. Сохранения замен по датам ещё нет.
- [x] Строка нагрузки без учителя больше не выпадает из расписания молча
- [x] **Надёжность солвера — закрыто.** Узкое место — физкультура (72 урока на 72 места
      в залах пн/ср/пт); дни заданы явно, черновику 80% бюджета, доводке потолок нарушений.
      5 из 5 прогонов — 0 нарушений за 120 с, первая сетка ≤ 3 с.
- [x] Печать A4 по классам и по учителям (из дня 4)
- [x] Кабинеты не «прыгают»: у класса свой кабинет
- [x] Excel данных школы: скачать/загрузить (наш шаблон; чужая тарификация — нет)
- [x] Пример в веб-приложении обезличен: название школы скрыто (ФИО в файле и так вымышленные)
- [x] Замок на всё приложение: переменная `LAD_PASSWORD` (базовая HTTP-авторизация браузера,
      `/api/health` открыт). **На Railway задать обязательно** — это и есть «общий демо-доступ»
      из порядка урезания; вход по почте не делаем.

### День 1 — вторник 15.09 (Startup Day)
- Утро: показ на Streamlit.
- API: школы (создать, открыть, сохранить), запуск составления с прогрессом
  по SSE и остановкой, версии расписаний, проверка ручного хода.
- Фронт: оболочка приложения, дизайн-система (типографика, цвета, сетка),
  экран школы со сводкой.

### День 2 — среда 16.09: сетка и ручная правка — главный экран
- Сетка по классам, учителям и кабинетам.
- Перетаскивание урока. Пока урок тянут, каждая клетка подсвечена:
  зелёная — можно, жёлтая — можно, но хуже (+окно у учителя),
  красная — нельзя.
- Отпустил в красную — панель «почему нельзя» человеческим языком,
  со ссылкой на пункт нормы: «Петрова в это время ведёт 7Б», «физкультура
  второй день подряд — п. 94 ССЭТ № 525».
- Отмена/повтор. Закрепить урок → «пересобрать остальное» (есть в ядре — `pinned`).
- Живые три цифры сверху пересчитываются после каждого хода.

### День 3 — четверг 17.09: ввод данных
- Импорт нагрузки из Excel: наш шаблон плюс терпимый разбор тарификации
  (ФИО, класс, предмет, часы, подгруппа) с отчётом «что не распознано».
- Таблицы классов, учителей, кабинетов и нагрузки с проверкой на лету.
- Пожелания учителей кликом по сетке недели.
- Проверки до запуска (уже есть в `tables.check_norms` и `build_school`).
- **Отправить ссылку завучу** — первый живой пользователь (§8.2 CLAUDE.md).

### День 4 — пятница 18.09: замены и выход
- Замены: отмечаешь учителя отсутствующим на дату, система ранжирует
  кандидатов на каждый урок (свободен в этот час, ведёт этот предмет
  или этот класс, не создаёт окон) и объясняет выбор. Лист замен на печать.
- Экспорт: Excel (есть), печать по классу и по учителю.
- Вход: почта и пароль, одна школа — один аккаунт, приглашение по ссылке.

### День 5 — суббота 19.09: полировка и заморозка
- Первый экран и пустые состояния, демо-школа в обезличенном виде.
- Прогон на гимназии (28 классов) и школе завуча; багбаш по полному пути.
- Правки по ответу завуча.
- Сценарий показа и 60-секундное видео.
- Заморозка кода к вечеру.

## Что режем, если день съехал

По порядку, не торгуясь:
1. Вход по почте → общий демо-аккаунт с паролем.
2. Замены → переносятся за 19.09.
3. Разбор чужой тарификации → только наш шаблон Excel.

**Ручная правка с объяснениями и надёжный солвер не режутся:** без них это
не продукт.

## Чего нет в этих пяти днях

- LLM-ввод пожеланий голосом/текстом — после 19.09, если всё остальное готово.
- Интеграция со schools.by и «Электронной школой».
- Мобильное приложение, вторая смена, несколько школ у одного пользователя.
- Биллинг.

## Открытые решения (нужен ответ Влада)

1. Railway платный (от ~$20 в месяц за 4+ vCPU) — ок?
2. Домен — нужен к 19.09 или хватит адреса `*.up.railway.app`?
3. Кому показываем 19.09: министерству, ГИАЦ или завучу — от этого
   зависит, что полируется в последний день.
